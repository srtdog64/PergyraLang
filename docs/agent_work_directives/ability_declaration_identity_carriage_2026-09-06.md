# Ability declaration callable identity carriage

Status: IMPLEMENTATION COMPLETE for the ability-ID regression (primary only);
published as `426cd694c03107d462f47f1050e9a5dab83e6278`. Broader generic-member
direct-MIR findings remain OPEN under their separately named bounded directive.
Base: `16b2f894cec5e0478639d68e89a6fb98e9168dea`

## Objective card

- Objective: restore the reached valid ability-bind source through the existing
  self-host MIR/canonical consumer; expected executable output is `12`.
- Priority: stable semantic identity, owner-directed carriage, no reconstruction
  fallback, executable negative evidence, then patch size.
- Fact owner: semantic callable signature SyntaxNodeId; MIR declaration method
  rows carry it without inventing an implementation identity.
- Last consumer: `MirExpressionIdentityEpochFromArtifactWithSignatures`, then
  the existing expression graph rebind/admission path.
- Forbidden fallback: dropping/zeroing IDs, numeric-offset inference, mapping
  an ability signature to its implementation, AST rescans, or name-only call
  resolution after an exact identity join fails.
- Integration gate: the existing ability-bind driver rung2 parent must execute
  `12`, followed by missing/unknown/duplicate/wrong-owner method-ID refusals and
  declaration-order / same-method-name controls. A valid-input refusal is RED.

## Scope and budgets

Primary owns the MIR method ID producer/serializer/verifier, its native wire
projection counterpart, canonical epoch consumer, and focused parity gate.
The first executable candidate restored ability dispatch but exposed the five
strict direct-MIR method schemas. Their field counts and the common routine
admission join are part of this same wire-consumer migration, not a new rung.
Runtime method IDs are checked against their declarations at routine-index
capture; compile-time erasure validates and hashes its signature ID.
There are no delegated implementation lanes and no overlapping agent leases.
Class receiver semantics, keyword removal, unrelated enum findings and general
SoT cleanup are outside this slice. Existing dirty review/navigation documents
are preserved; they are not semantic authority.

Read-only inspection, isolated current-source builds through the Pergyra seed,
and focused gates are allowed. Static checks have a 60-second budget, focused
parity five minutes, and an integration build/shard 30 minutes. No full matrix,
cap increase, timeout increase, installed-binary overwrite, or second build is
implied by an observation timeout. Storage reclamation is complete; preserve
the original failing MIR and all protected seed/fixed-point evidence.

## Evidence

The original counterexample is preserved in
`../audits/2026-09-06_ability_declaration_identity_epoch_counterexample.md`.
Edits in this scope are implementation candidates until their gates run.
No substitution percentage or registry status changes with this directive.

Initial candidate `ED98930E` passed the existing hard ability parent and nine
refusals / three runtime controls (`2974ee`), the canonical identity gate
(`7cfa6b`) and generic specialization epoch (`77abe2`). Its strict role-override
consumer refused the new method field (`1c16aa`); it is not the final candidate.
The adjacent bound test also retained the old full-object wire pin (`eeef3e`).
The successor build remains in the same isolated workspace scope.

Final candidate `7E702159` / graph `485cef98` passed its seed build, seven-case
hard parent, exact ability-ID negatives and controls, canonical and generic
epochs, isolated installed-layout role override, declaration erasure and role
operator C/LLVM gates. Native MIR unit execution passed 162/162. Exact hashes,
receipts and omissions are in the counterexample audit's executed-repair section.
Inferred/constructed generic direct-MIR identity refusals were reproduced with
the unchanged prior driver and remain explicit valid-input bugs. Full Platform
green, shared installation and new fixed-point evidence are not implied.

## Post-publication native-oracle integration repair

Regular CI `33994165910` on published `426cd694` is FAILURE, 28/30. Linux
bootstrap's first native-oracle error is a borrowed aggregate copied from
`routines.declarations` into a local `declarations` binding in the new epoch
method admission. The same full driver native C emission fails locally with
that first error and 26 subsequent TextBuilder errors (`1dfd3d`). Removing only
the borrowed aggregate alias makes the same full emission pass with zero
errors/four existing warnings (`52a9ca`); the generated C also compiles
(`9afe79`). All 26 subsequent errors disappear in this reproduction, so they
are cascading evidence here, not 26 independently repaired defects.

Primary alone resumes this reached consumer. Objective: consume the existing
borrowed declaration index in place. Priority: reference lifetime, exact IDs,
then executable parity. Fact owner: `routines.declarations`; last consumer:
`MirExpressionIdentityEpochAppendDeclarationMethods`. Forbidden fallback:
disabling reference checking, copying the whole index, rebuilding declarations
from AST/name scans, or suppressing the native oracle. Integration gate: the
same complete driver native C emission followed by the focused ability parent
and canonical identity gates. Existing time/cap/storage budgets remain fixed.
The generic receiver slice is locally green and has no simultaneous edit lease.

Final integration candidate `FC669FD2`, graph `d1571d67`, also passes the
complete native-oracle C emission/compilation (`6fd83b`), seven-program hard
parent (`1ab2ce`), ability controls (`b2bbce`), identity epochs and isolated
installed-layout role gate (`1f9300`, `30255b`). The reached test-cap and
batch-diagnostic repairs pass the complete structural inventory (`f06a35`).
The bounded follow-up is locally verified; remote CI and publication are pending.
