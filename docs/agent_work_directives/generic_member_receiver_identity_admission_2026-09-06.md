# Generic member receiver identity admission

Status: LOCAL IMPLEMENTATION VERIFIED; scoped publication and remote CI pending
Base: `426cd694c03107d462f47f1050e9a5dab83e6278`

## Objective card

- Objective: restore the reached inferred and constructed generic member
  direct-MIR C/LLVM programs without discarding their formal parameter IDs.
- Priority: semantic identity, existing owner-directed admission, missing-fact
  refusal, executable negative evidence, then patch size.
- Production entrypoint: `driver_bootstrap_main.pgy --mir-json-backend` through
  the existing compiler world and direct-MIR member classification.
- Fact owner: the MIR formal parameter `source_syntax_id` and the existing
  `MirRoutineHeaderFacts` parameter rows. The common generic member receiver
  admission consumes this fact before bounded signatures and plans are sealed.
- Last consumer: `DirectMirGenericMemberReceiverParamReady`, shared by inferred
  and constructed generic member signature admission.
- Forbidden fallback: accepting the old nine-field receiver, removing IDs,
  accepting partial/duplicate formal identity rows, defaulting a missing ID,
  retrying native C, or interpreting this input as a different program family.
- Integration gate: the existing inferred and constructed one-MIR projection
  gates must execute their unchanged class/vessel/constructed fixtures through
  C and LLVM, with missing, malformed and colliding receiver identity refusals.

## Reached falsifier and scope

The prior/current drivers both reject the unchanged inferred/constructed
fixtures, as recorded by the ability-ID audit (`b7b5a3`, `1efd4a`, `b7366b`,
`76eb8a`). Current producer receiver rows contain ten fields including
`source_syntax_id`; the common receiver validator still requires exactly nine.
The constructed signature calls that same validator. This is a named stale
wire consumer, not a second implementation lane or a missing syntax feature.

Primary owns only that common admission, its two immediate signature consumers
where necessary, the existing two focused test gates/mutation generators, and
evidence/navigation updates. No sub-agent holds an edit lease. Other class
receiver semantics, keyword proposals, enum findings, unrelated SoT families,
installed binaries and existing counterexample artifacts are out of scope.

Use isolated current-source outputs and preserve the published ability-ID
candidate. Static checks have a 60-second budget, focused parity five minutes,
and an integration build/shard 30 minutes. Do not increase caps or timeouts,
introduce a cache, or run a full matrix to diagnose this bounded admission.
The primary is the integration/publication owner. Source inspection is an
observation; a patch is a candidate until the actual gates run. Restoring this
previously reached path alone does not increase substitution percentages.

## Observed focused result

### Reached literal-Log structural integration scope

The full component gate reaches a 565/560-line family after the receiver test
caps are repaired. Primary's bounded follow-up is the existing C emitter and
its declaration/literal projection gate, within this same publication lane.
Objective: remove duplicated formatted-call construction without changing the
verified literal plan or emitted bytes. Priority: plan/ABI identity, unchanged
failure and execution behavior, then size. Fact owner: `DirectMirLiteralLogPlan`
and its `formatted_print` runtime ABI fact; last consumer:
`DirectMirLiteralLogEmitC`. Only argument spelling and the integer temporary
are variant-specific. Forbidden fallback: reopening MIR/AST, deleting admission
checks, changing the ABI, adding a formatting owner or raising existing caps.
The integration gate is the existing declaration/literal C/LLVM projection gate
with an added string runtime control, before/after artifact equality, and the
unchanged full component inventory. A fresh Pergyra-seed driver must cover the
final source; the old binary is only a comparison oracle.

### Receiver result

The repeated full-inventory cycle exposed the batch checker's immediate exit
after its first line-cap failure. The reached follow-up keeps the existing
request owner and every cap, but collects independent unreadable/oversized-file
diagnostics in `run_line_cap_checks` and returns failure once. Invalid request
construction still fails immediately. The actual checker-mechanics gate must
reject a batch containing two oversized files and a missing file, report all
three, and retain exit 1 plus all existing success/negative controls. Primary
also owns the reached role mutation generator's 108/100 cap repair; all emitted
fixtures must remain byte-identical and its existing execution gate must pass.
No new compiler owner or parallel implementation lane is opened.

Candidate `B9EDBB8F71C41A1FA9C6049AF1373E26A4634224926BD41EA8EEE5400F12B350`
was built through the Pergyra seed (`fdd50b`), graph
`e554a3d9895c01d4d99814feb15f9f67a462256bc8700df89b32ddac18759cf0`.
The existing inferred gate passed class 41 / vessel 42, both C/LLVM, with
97 C negatives and 25 LLVM sentinels (`b48807`). The constructed gate passed
43, 48 C negatives and 13 LLVM sentinels (`24eea1`). Changed but valid unused
receiver IDs preserve both emitted artifacts. The prior published candidate
accepted the receiver-ID-deleted input and emitted C (`3b6006`); the new
candidate refuses it. All new ID refusals have the exact family-owned identity
diagnostic, exit 1 and no artifact (`363e88`). The final scripts assert that
exact message explicitly; their combined-candidate reruns also passed below.

The combined native-oracle follow-up candidate `55818B29` passed both final
member gates (`75204f`, `424af6`), the seven-program hard parent (`84c1b2`),
ability refusals/controls (`cb6be5`) and both identity epochs (`6b056c`).
Existing test caps now fit: inferred shell 200/200, inferred mutations 358/360,
constructed mutations 332/350. The shared 51/80-line parameter-identity test
module removes duplicated comparison/mutation mechanics, not compiler facts.
All 244 generated inputs retain identical paths and SHA-256 values (`0b7a41`);
six actual comparator negative controls passed (`eee805`).

The publication-boundary inventory exposed the existing literal Log family
at 565/560 LOC (`aee960`). Common formatted-call emission removes five lines;
the unchanged cap now holds at 560. A new string runtime control and all 28
existing negatives pass on old/new candidates, with twelve byte-identical
C/LLVM artifacts (`86703c`, `b200c2`). The next full run reached its final line
batch and found the published producer harness at 321/320 (`c8fae2`); folding
only the ability-call continuation keeps the same command and arguments at
320 lines. No check was removed or cap increased.

Final candidate `FC669FD2`, graph `d1571d67`, passed the Pergyra-seed build,
native oracle emission/compilation, inferred/constructed C/LLVM gates and both
identity epochs (`575a9d`, `6fd83b`, `5d6465`, `7618b5`, `1f9300`). The final
seven-program hard parent passed (`1ab2ce`). The role generator now fits
100/100 with all 17 inputs byte-identical (`ddf718`), and the existing isolated
installed-layout role gate passes (`30255b`). The actual batch checker now
reports all independent failures; its new control failed before and passes
after the repair (`4cbdef`, `938dfd`). Complete component run `32435` passed
(`f06a35`), followed by documentation and exact-scope checks (`521164`,
`5146a1`). Publication is pending. No shared installed binary, status or score changed.
