# Integrated Publication Review

Status: AUDIT COMPLETE; primary publication/remote integration remains pending.

Base: `b4d22cf2c6e68fcbd42a1ce4a44444de0e7899fa` plus the existing dirty work.
The user explicitly authorized primary to integrate other-session changes and
continue through publication/CI. This reopens their review scope, not a new
compiler architecture or implementation rung.

## Shared objective card

- Objective: publish a coherent, evidence-backed integration of the current
  enum/receiver self-host replacement, reached CI/transport fixes and reviewed
  documentation/test changes from the other session.
- Priority: preserve semantic owners and explicit limitations, preserve user
  work, verify reached behavior, then deliberate Git publication and CI.
- Fact owners: existing declaration/MIR/GraphPlan owners and installed-driver
  fixed-point receipt; concurrency canon/runtime and existing fixture goldens
  own that bounded evidence. Research notes are references, not compiler SoT.
- Last consumers: installed public/self-host projections and existing parity,
  diagnostics, CI profile and example gates.
- Forbidden fallback: add-all publication of generated C/MIR, silently revised
  goldens, native retry for a self-host refusal, model/reference-as-implementation
  claims, lost other-session edits, or another compiler implementation lane.
- Integration gate: narrow changed-scope checks followed by CI and Platform
  full at the exact pushed revision. Local reports do not replace remote gates.

## Independent scopes

- Primary: all source/test/document integration edits, staging/commits/push,
  workflow dispatch and result inspection. Primary also classifies root scratch
  artifacts and preserves originals; no broad cleanup is delegated.
- `ci_assertion_review`: review previously separate concurrency changes:
  docs/204, docs/concurrency/Main.md and advanced_examples.md, the reconciliation
  audit, async positioning gate, concurrency example gate and eight new
  parallel-case expected.stdout files. Run existing narrow positioning and
  example gates against the isolated native below. Edit only
  `docs/audits/2026-09-05_integrated_concurrency_publication_review.md`.
- `semantic_index_review`: review the eight existing files under
  docs/compiler_architectures for local-link integrity, stated provenance,
  current-owner versus historical/reference qualifications, and accidental
  implementation/progress claims. Do not conduct a new general research project.
  Report precise corrections needed for publication, distinguishing source
  checks from unverified external claims. Edit only
  `docs/audits/2026-09-05_architecture_notes_publication_review.md`.

Agents may read all paths in their scopes because the user reopened the former
other-session exclusions. They must not edit shared source, tests, navigation,
each other's reports, or Git state, and must not delegate further.

## Commands and budgets

- Normal execution now works with full filesystem/network access; do not pass
  sandbox_permissions. All authored file edits still use apply_patch.
- Read-only source/diff/link checks; static gates at most 60 seconds, one focused
  example run at most 300 seconds. No full matrix, compiler/self-driver rebuild,
  full MIR production, package installation or deletion by an agent.
- Native oracle: `.tmp/native_await_integration_20260905/pgy.exe`, SHA-256
  `2E0468AAE6521FE804C08ABE679EAF3A166CA1624C208774EE2142D3C870EDCE`.
  Example verification is explicitly native, not self-host progress.
- D: has about 88 MiB free. Create only the gate's small bounded artifact set,
  preserve existing evidence, and report insufficient storage instead of
  retrying, enlarging budgets or removing files.
- Findings are observations/proposals; report a blocking finding promptly.
  Primary accepts corrections against current source and owns the final gate.

## Observed integration handoff

- Both agents completed their separate reports under `docs/audits/`. Primary
  integrated the wording corrections and fixed the exact pre-existing Korean
  README relative link. Historical architecture counts and unrefreshed upstream
  descriptions are not promoted to current evidence.
- Native concurrency example gate: 16 C/LLVM executions and 10 artifact-free
  semantic refusals PASS in `.tmp/concurrency_examples.bhvG0K/`. Positioning and
  separate shell syntax checks PASS; existing `parked`/GCC warnings remain.
- Primary current-source fixed-point receipt revalidation, documentation quality
  and CI profile checks PASS (`21019`). Earlier full comparator and installed
  enum/receiver evidence is recorded in the active handoff, not rerun by agents.
- Final local static integration (`59948`) PASS: documentation, SoT edge,
  keyword inventory, runtime-twin border, component checker mechanics, semantic
  frontier refusals and separate syntax checks of all changed shell scripts.
  Scoped strict UTF-8 passed for all 143 publication inputs. Staged whitespace
  inspection found extra EOF blank lines in five pre-existing architecture
  notes; primary removed only those blank lines before publication.
- Generated root C/MIR workspaces are preserved and narrowly ignored; the root
  source probe remains untracked. Neither agent staged, committed or published.
- The audit's completion is not remote CI success, a closed SoT row, or a new
  implementation rung. Primary retains exact-revision CI/Platform full checks.
