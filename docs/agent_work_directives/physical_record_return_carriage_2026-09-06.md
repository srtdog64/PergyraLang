# Physical record return carriage

Status: LOCAL INTEGRATION COMPLETE — C681F4BF passes the original assignment C/LLVM gate (session 92648, exit 0, `2f66c7`).
Primary is the only integration/edit owner; no current-source full fixed point is claimed.
Use the active handoff and this directive's final observation for the live job.
Earlier candidate/next-step entries below are historical evidence, not a queue.
The outer goal continues at `current_source_bootstrap_2026-09-06.md`; that full
bootstrap verification is separate from this card's completed integration.
Base: `5b97f2e10ffa7ecf9cfe932829a83ffffaa3ba12`.
Outputs are implementation candidates until the executable gates below pass.

## Objective card

- Goal: the production self-host LLVM path must execute the real entrypoint
  selection owner's record return and copy-out without discarding its physical
  nominal ABI receipt. The full assignment-projection probe is the integration
  falsifier, not a different implementation track.
- Priority: semantic identity, exact producer receipt, owner-directed projection,
  missing-fact refusal, then patch size. No percentage change follows from this
  directive or from new owner files.
- First-class facts: declaration/field source identity, required-vs-absent ABI,
  exact layout row, scalar storage width, callable carriage and copy-out identity.
- Fact owners: `nominal_abi_layout_fact_owner.pgy` still produces physical rows;
  `direct_mir_nominal_declaration_abi_fact_owner.pgy` admits them. The existing
  logical-record inventory continues to own passive record/field identity and
  gains an explicit, sealed layout carriage. It must not claim physical absence
  for a required row. Parameter admission must retain enough evidence for exact
  declaration-row joins after the original JSON is retired.
- Last legitimate consumers: the scalar GraphPlan's callable/instruction
  admission and the C/LLVM record target projections, including copy-in/out and
  field construction/read/rebind. Emitters consume carried field layout; they
  may not infer it from the compiler type's name or source AST.
- Allowed bridge: the existing native launcher selects the isolated Pergyra
  driver. Logical-only records retain their existing target representation;
  producer-required records use their admitted physical representation. These
  are explicit evidence states, never a failed-new-path/old-path retry.
- Forbidden: removing required ABI, pretending it is absent, a four-field or
  compiler-type spelling special case, unsupported struct-array storage in the
  compiler, widening a two-Int shape into an unproved general layout, native
  codegen fallback, skipping LLVM, or weakening missing-fact controls.
- Budget: static owner gate 60 s, focused parity 5 min, integration shard 30 min.
  Capture bounded per-row evidence once; no program-global text reconstruction
  inside routine emission. Preserve existing binaries and failing artifacts.
- Positive gate: a source fixture importing the actual
  `semantic/ast_entrypoint_selection_policy_owner.pgy`, returning its four-Int
  accumulator, observing a candidate and finishing selection, must execute the
  same result through public C and LLVM and direct verified-MIR projections.
  Additional field-count, nested-record, negative-Int and non-first-parameter
  controls distinguish general owner carriage from a fixture-shaped patch.
- Negative gate: missing/changed declaration and instruction ABI, parameter
  receipt drift, layout/field identity disagreement, and a forged physical
  receipt on a logical-only record must fail before artifact publication.
- Integration: `tests/self_hosted/parity/assignment_projection_probe_parity.sh`;
  retain logical-record and two-Int nominal parity gates as regression evidence.
- Non-goals: keyword deletion, retry semantics, general evaluation-order work,
  CI workflow redesign, module build, shared installation and a new fixed point.
- Stop/reject: do not publish a relaxation before target width/copy-out evidence
  and negative gates are observed. A newly reached failure remains open evidence.

## Edit scope and coordination

Only primary edits the passive-record/parameter ABI carriage and reached target
consumers, their focused tests, and the corresponding registry/handoff evidence.
No subagent or separate implementation lane is opened. Previous seven dirty
documentation paths are preserved; unrelated changes remain outside this scope.
Allowed commands are scoped source inspection, isolated candidate build, focused
C/LLVM execution, structural/documentation gates and existing CI observation.
Do not overwrite shared `bin/` or dispatch another full CI matrix during the
focused loop. Publication requires a reviewed diff and observed relevant gates.

## Starting evidence

The preserved real-owner import probe runs as C (output `1`) and fails LLVM at
`callable-route-envelope`, `return-type`, routine 0. Its accumulator has required
layout ID `577017080`, four 4-byte Int fields, size 16/alignment 4. The current
logical record backend instead uses i64/int64_t fields and assumes no physical
ABI. Relaxing only declaration admission would therefore be unsound. A control
without inout also receives a physical receipt: inout is not its producer.

## Reached consumer migration

Candidate `970D7619` built through the Pergyra seed and executes the preserved
real-owner LLVM probe with exact output `1` (`06790a`). The expanded fixture then
exposed two different C types for the same two-Int declaration (`d90e66`): the
old scalar-program nominal branch and the record inventory both claimed it.
The fix therefore removes the scalar GraphPlan's separate nominal field and
four two-Int-only fact/target/preamble files; callable, instruction and target
consumers use the record inventory. The shared two-Int physical shape owner used
by other bounded routes is outside this deletion scope. This migration is not
complete until the expanded fixture and previous nominal regressions pass.

## Current executable checkpoint

Final isolated `958AE778` / source graph `c415b3d8` builds through the Pergyra
seed and passes the expanded public/direct C/LLVM fixture and 24 owned receipt
refusals. Two-Int, logical-record and Option<Int> regressions pass. The retired
nominal path cannot return through the changed component-inventory slice.
See the assignment-projection audit for complete hashes and observed receipts.

The original compiler-scale LLVM leg now passes the accumulator return boundary
but ends with extension readiness code 19: the existing owned Array<String>
move fact/coverage or its Array<String> ABI join. Its exact failing row remains
unidentified. The run ended itself, no manual process termination occurred, and
no run remains live. Reduce this same-rung falsifier at the current owner before
another compiler-scale execution. Keep the observed parameter/enum revalidation
cost as bounded evidence; it does not authorize a general cache/query system.

## Reached owned-move objective card

- Objective: preserve a caller-local Array<String> ownership transfer using
  the admitted callee/parameter identity, not its position in the program.
  An independent return value after the transfer must not count as a use of
  the retired local. The full assignment program remains the integration goal.
- Priority: exact identity and ABI, last-use/coverage proof, cleanup ownership,
  explicit rejection, then implementation size. No new ResourceCarriage model.
- Owner: `DirectMirScalarProgramOwnedArrayStringMoveFactFromProgram` consumes
  callable inventory and existing graph-local identity; its coverage owner and
  final GraphPlan check prove retirement. The Array<String> ABI owner is unchanged.
- Last consumer: C/LLVM per-local cleanup policy consumes the sealed move fact.
- Forbidden: arbitrary-callee permission without inventory admission, ignored
  ABI, unconditional cleanup suppression, accepted use-after-move, or changing
  the actual compiler's consuming call into a borrowing call to pass the gate.
- Falsifiers: identical four-routine sources differing only in callee order;
  an unrelated String return after a move; a real codegen-owned consuming join;
  returned access to the moved local, duplicate transfer and missing ABI.
  All three rejected positives currently reach code 19 on `958AE778` (`f5738c`).
- Scope boundary: this first reached correction does not establish consuming
  calls nested inside return expressions or general branch/loop coverage. Those
  production cases remain in the same integration objective and must be reduced
  before another whole-program run. Do not call the compiler-scale rung closed
  just because callee-order and independent-return controls pass.

### Prior owned-move checkpoint — superseded by the complete-exit proof below

Isolated Pergyra-seed candidate `613AE6CD` / source graph `11aac061` is built
and source-hash verified (`da8946`, `749f2a`). The actual join/CommitRoot fixture
passes public/direct C/LLVM with consuming returns, strict nested/nary calls,
pre-call loops and no-use post-call branches/loops: 14 runtime lines and 24
refused projections (`e05d85`). Five repaired-digest mutations of a real issued
GraphPlan reject stale flow coverage (`0575e2`). Existing move/callee-order and
physical-record regressions pass (`a1fce5`, `c86f1d`).

The next same-rung falsifiers are now `two_consuming_exits_owner.pgy` and
`consuming_record_constructor_owner.pgy` under
`.tmp/self_hosted/owned_array_move_20260906/`. Both reduce the production
`EmitStmtList` path and both reject C/LLVM code 19 on this candidate
(`c2ea7f`), evidence `.tmp/self_hosted/owned-array-multiple-exits.WLyfFc/`.
Before the next implementation, extend the objective card explicitly for the
complete consuming-exit set and constructor operand identity. Do not interpret
the first successful exit as coverage of all exits, or treat arbitrary expression
parents as guaranteed evaluation. No whole assignment rerun is warranted while
these concrete reductions are red. Shared binaries, index and published HEAD
are unchanged; this entire integration scope remains uncommitted.

## Reached terminal-move objective card

- Objective: execute the actual compiler's consuming return and nested join
  calls, including loops before the consuming exit, through the same owned
  Array<String> move fact. The original assignment projection is still the
  integration target; the small sources are falsifiers, not a replacement goal.
- Priority: exact graph/caller/block identity, last-use and exit-path coverage,
  ABI and one cleanup owner, explicit refusal, then compact implementation.
- Owner: the existing move admission collects admitted call/parameter/local
  identity. Its coverage owner gains a terminal-exit proof: one local definition
  dominates the consuming block and all terminating paths after that definition
  pass the consuming block, without re-entering the definition. Earlier returns
  before the definition need not consume an uncreated owner. Cycles before the
  exit may remain; repeated consumption in a loop is not admitted by this proof.
- Graph role: operation and terminal-return graphs are explicit disjoint uses,
  not a missing-operation fallback. Nested consumption needs a single guaranteed
  evaluation path through the reached strict call/Concat parents; conditional,
  duplicated or orphan call occurrences are refused.
- Proof lifetime: coverage binds the exact typed flow input with a digest. The
  final GraphPlan compares that carried input identity using borrowed array
  views; it does not reconstruct a second independent CFG proof. The existing
  expression/ABI identities remain authoritative. No AST or JSON re-scan.
- Last consumer: existing C/LLVM local cleanup consumes the sealed move fact;
  neither backend receives a new cleanup decision or fallback.
- Gates: actual consuming join and CommitRoot imports in C/LLVM, direct return,
  nested call, pre-call loop, pre-definition early return, and all prior move
  controls. Negatives cover bypass exits after definition, a moved-local read,
  duplicate/repeated consumption, lazy or duplicated graph evaluation, missing
  caller/graph/ABI identity, and stale coverage after a typed-flow mutation.
- Scope: only the reached Array<String> caller-local move. No generalized
  ResourceCarriage, query engine, runtime flag, new syntax, or native fallback.

### Reached post-move continuation (same ownership proof)

The consuming return/nested/loop/branch fixture now passes on `D953912A`
(`1cbf4e`). The compiler's `function_emit.pgy` also consumes a join before
subsequent branches. Its reduction `post_move_branch_owner.pgy` rejects both
targets at code 19 (`15bd7f`); the pre-move process-fatal control projects both
targets (`ecf684`). These are observations, not whole-rung completion.

Extend the same exit-coverage proof to this reached nonterminal call: the
definition dominates the move, every terminating path after definition reaches
the move, and all reachable operations/conditions/returns after the move have
no use of the retired local and cannot re-enter its consuming block. The existing
typed input digest carries these exact edges and uses; final readiness must not
derive a second proof. A branch/loop after the call is not itself an ownership
failure. Negative gates must cover both successor-arm use and repeat entry.

## Active complete-exit-set objective card

- Objective: execute the real `EmitStmtList` ownership shape: one caller-local
  Array<String> consumed exactly once through either of its two exits, including
  a consuming call under the admitted record constructor. The original LLVM
  assignment projection remains the integration target.
- Evidence: both reduced inputs again reject code 19 on `613AE6CD` with valid
  output arguments (`79161a`). An earlier resume command omitted `-o` and only
  tested CLI usage (`428751`); it is not ownership evidence. Forwarding an owned
  parameter is already refused on this baseline (`160205`); this change must
  not invent parameter-origin authority to admit that separate shape.
- Priority: exact caller/local/call/parameter identity, exactly-one exit-set
  coverage and no later use, single cleanup/ABI ownership, deletion of old
  shape-specific proof authority, then representation size.
- Owner: existing move coverage admission groups rows by caller/local identity.
  It proves definition dominance and coverage by the complete consuming-block
  set once per owner. Every consuming site separately proves its strict single
  evaluation, local last use and absence of reachable later uses or consumption.
  Different admitted owned callees/parameter ordinals need not share a name;
  each row still joins its exact callee/parameter and ABI.
- Constructor boundary: consume the already admitted logical-record operand
  rows, field arity/types and strict evaluation. Do not admit arbitrary parents,
  orphan/duplicate occurrences, or rebuild constructor identity from spelling.
- Representation: retain the same move/coverage fact identities and typed-input
  digest. Replace straight/four-block/control-flow tags and branch/merge arrays
  with one complete-exit coverage claim. Remove the old one-block and four-block
  proof implementations; no `new ? old` proof fallback remains.
- Receipt coupling also includes the derived consuming-block rows themselves;
  repairing both inner receipt digests and the outer plan digest must not make
  a post-issue block-row change into a new proof. The gate-only probe checks
  this explicitly alongside its previous five typed-input mutations.
- Last consumer: unchanged C/LLVM local cleanup policy, after the final GraphPlan
  has checked the carried typed-input identity. No backend proof reconstruction.
- Gates: original four owned-move controls, callee-order and prior covered-flow
  controls; a two-exit loop, three exits, different admitted callee/parameter
  destinations, nested 2/3-field constructors; missing-exit, sequential/repeated
  move, sibling/condition use, duplicate constructor evaluation, missing operand
  and identity/ABI refusals; then the exact original compiler-scale LLVM rung.
- Scope/coordination: primary only, no new compiler module, runtime flag, general
  ResourceCarriage or native fallback. Existing file caps remain. Static gates
  get 60 seconds, focused parity 5 minutes, compiler-module/integration work
  30 minutes. Compiler source stays frozen while an import-composed build runs.

### Complete-exit executable checkpoint

Final isolated Pergyra-seed candidate `0CDAF013` / source graph `c2254aee`
builds (`b3ff6c`) and matches all recorded source hashes after generated-inventory
refresh (`d9b61f`). The two prior reduced RED forms execute C/LLVM on intermediate
`A3AFBE8D`; the final expanded fixture executes 24 expected lines on every public/
direct C/LLVM path and refuses 34 negative projections (`bebe72`). The actual
issued-plan probe rejects six stale-input mutations, including repaired inner
and outer digests, then accepts exact restoration (`a69466`). Old move gates,
callee identity/independent-return and physical-record regression gates pass
(`e70396`, `1bb106`, `fd7aa2`); their runtime assertions were not weakened.

The exact original assignment-projection gate is now running both backends on
this candidate under its 30-minute integration budget, session `44755`.
Compiler source is frozen. The current handoff owns its live/terminal status;
the earlier code-19 outcome does not predict this result. Do not install, publish
or promote completion before the actual integration result is observed.

## Reached full-MIR identity hashing objective card

- Evidence: live stack `df9b51` reached `DirectMirScalarCfgSealGraphPlan` through
  `DirectMirCfgMirDigest -> DirectMirCfgHashString -> pgy_strlen -> strlen`.
  Extension admission has already checked the former code-19 predicate before
  this call (`1b74c7`). Source and emitted C both compute StringLength once for
  the seed and twice per byte in the hash loop (`c65cad`, `097afa`).
- Objective: compute the same immutable String length once per hash invocation,
  preserving the exact byte recurrence, seed, modulus, bounded digest and owner.
  This removes the reached repeated full-string scan; it is not a global cache
  or a memory-erasure claim. The original integration is still the falsifier.
- Priority: byte-identical identity, same owner/last consumer and failure
  behavior, bounded linear work, then implementation size.
- Owner: `src/self_hosted/air/mir_cfg_identity_owner.pgy`; last consumers remain
  certificate/GraphPlan sealing and verification. No schema, top-level fact,
  new identity authority or backend-specific hash is permitted.
- Forbidden: skipping input bytes, a weaker hash, trusted-input bypass,
  memoization keyed by a mutable pointer, or increasing integration time/memory
  limits to hide the repeated scan.
- Gates: the old repeated-scan body must fail the focused structural check;
  importing the actual owner must execute empty, ASCII, escaped and UTF-8 values,
  modulus-edge seeds and a 1 MiB deterministic value against an independent byte
  recurrence. Inspect emitted C for one length computation, retain ownership
  regression gates, then repeat the original integration on a rebuilt candidate.
- The exact integration child was deliberately stopped after this diagnosis
  (`d4ed5c`); session `44755` is terminal exit 1 (`26517d`), with the launcher's
  child-termination code -1. It is neither code-19 rejection nor a timeout/PASS.
  Do not restart the same candidate. Compiler-source freeze is now released.

### Linear-identity candidate checkpoint

Pergyra-seed build `97911` completed PASS (`af0a9f`). Candidate `908D9A4E` /
source graph `52cf237e` matches its recorded inputs (`f7fa15`, `6cd0bc`); the
previous input graph differs only in the existing hash owner (`8e0794`). On
this binary, actual-owner hash, terminal-flow runtime/34 refusals, physical
record runtime/24 refusals and six sealed-flow mutations/restoration pass
(`560c8d`, `d2782c`, `731afd`). No registry authority or status changed.

The exact original C/LLVM assignment gate completed PASS on this candidate,
session `48391` (`b83a45`), under the unchanged 1,800-second integration bound.
Both runtimes have the same five output lines; C's seven and LLVM's six
missing-fact controls reject. LLVM compilation took about 21m25s, not a full
compiler performance closure. The previous stopped run has 256 hash-verified
preserved files (`fc8e67`); do not attribute this PASS to that older run.

## Reached native-oracle borrow-boundary objective card

- Evidence: the existing bootstrap gate's complete-driver native C emission
  fails before C compilation in the isolated `owned_array_linear_identity_native_oracle_20260906`
  directory (`191c00`, `dd6ac4`). First errors forward borrowed arrays into
  default-value hash parameters. The final plan consumer also returns a new
  aggregate containing borrowed plan arrays and binds a borrowed move fact.
  Later TextBuilder errors are retained but not assumed independent causes.
- Objective: the same admitted move proof and exact input digest must compile
  through both producer paths without a returned borrowed aggregate alias.
- Priority: preserve ownership and digest identity, delete the alias, keep one
  input-digest calculation, then representation size. Do not relax ref admission.
- Owners: the existing scalar-CFG array hash owner reads arrays through explicit
  ref boundaries; the existing coverage input-digest function owns the field
  order. Admission and final GraphPlan readiness pass their owned fields directly.
- Last consumer: final GraphPlan readiness compares the existing coverage input
  receipt; C/LLVM cleanup consumers and the coverage proof are unchanged.
- Forbidden: changing ref to own to silence admission, rebuilding/copying a
  whole graph, a second digest implementation, a native fallback, or installing
  the native-oracle artifact as the self-host driver.
- Gates: preserve the original failing log; actual-owner native C emission and
  compilation must pass, issued-plan stale-input mutations must still reject,
  and focused public/direct ownership/record gates must pass on a rebuilt
  Pergyra-seed candidate. Existing time/file caps remain; no parallel edit lane.

### Reached native summary correction

The first ref-only retry is terminal RED (`d325f0`). A seven-line `Count(ref
values: Array<Int>) { return ArrayLength(values); }` reduction independently
reproduces a helper-escape summary. Source inspection shows that the existing
`slot_analyzer_builtin.c` non-escape table omits ArrayLength, although its type
owner returns only Int and does not transfer the array. Binding length or an
element to a local does not correct that summary (`88f856`, `e6bb2c`); these
trial source changes were reverted, not retained as a workaround.

The same integration scope may correct that one builtin fact in the existing
native summary vocabulary and require an unresolved builtin call rather than
a same-spelled user declaration. No arbitrary pure-call inference or relaxed
unknown-call handling is permitted. Positive array/slice length summaries and
negative own forwarding/name-shadow controls join the existing semantic unit
battery; the native compiler is rebuilt into an isolated output directory.
The existing `semantic.function_param_flow_summary` registry row remains BRIDGE.

Native `A0F2FCDB` passes 2,877 semantic assertions and full-driver C emission/
compilation (`9d6f4b`, `c70e3c`). Pergyra-seed `B58EFE4E` / graph `c1090eed`
builds and passes public/native hash, terminal-flow and physical-record gates,
then bounded source-C/MIR producer/common-MIR consumer comparisons (`2332ae`).
The new native probe leg caught unsupported test-only source. Preserve those
logs; rebuild its immutable receipts with existing owners rather than changing
compiler field mutability. Native six-mutation/restoration execution passes
(`f3e78a`); final dual-producer session `9723` passes (`8df5c9`). Its gate-only
constructor companion is not a new compiler module or semantic owner. The
original assignment PASS belongs to `908D9A4E`, not the current candidate.

## Active readonly Array<Int> callable objective card

- Evidence: public LLVM rejects the actual hash fixture on `B58EFE4E` at
  `DirectMirScalarCfgHashInts`, parameter 1 (`474453`). A seven-line `Count`
  reduction executes `3` through public C and rejects LLVM at parameter 0
  (`445674`). Logs and verified MIR are preserved under
  `.tmp/self_hosted/owned_array_move_20260906/array_length_borrow*`.
- Production entrypoint: public source `--backend=llvm`, through the Pergyra
  scalar GraphPlan. The original assignment program remains the eventual
  compiler-scale integration target, not the immediate next invocation.
- Objective: carry the existing readonly Array<Int> parameter identity and
  physical ABI through admission, direct-call arguments, signatures and reads,
  without ownership transfer, mutation permission or cleanup suppression.
- Priority: exact declaration/parameter/carriage/layout identity, no escape or
  mutation, owner-directed target projection and negative controls, then size.
- Owners to inspect: existing routine signature/parameter facts and Array<Int>
  ABI; callable parameter policy and role plan; direct-call carriage owner;
  C/LLVM routine signatures and parameter-read/call-argument consumers. The
  existing Array<String> readonly family is reference evidence, not permission
  to assume both arrays have the same storage width or cleanup policy.
- Last consumers: C/LLVM argument and parameter-value projection, with ABI and
  lifetime decisions already admitted. No backend-local parameter rediscovery.
- Forbidden: ref-to-value/own substitution, accepting the signature without
  matching target bindings, copying whole arrays to evade borrowing, a parallel
  ABI authority, hidden mutation/copy-out, native fallback, or a large rerun
  while the reduced input remains red.
- Gates: minimal Count and actual hash-owner C/direct-MIR/LLVM runtime, correct
  parameter ordinals and reborrow/member cases as reached; missing/mismatched
  ABI and carriage, forbidden mutation/escape and copy-out confusion must fail.
  Retain String readonly and value/value-result regressions. Only after these
  pass should the original assignment-projection integration run once again.
- Scope: primary only, same integration rung. Preserve the current
  `B58EFE4E`/`A0F2FCDB` pair and receipts; implementation is now in progress.
- Source-backed implementation boundary: generalize the existing readonly
  Array<String> policy/target/C/LLVM family to the two reached Int/String types,
  deleting its String-only names rather than adding a parallel family. The
  existing Array<Int> program ABI fact admits readonly parameters without
  adding them to its value-result/copy-out subset. Retain the captured layout
  ID in its target projection and carry that projection from program emission
  to reads; do not derive it per expression. Element loads remain with the
  existing i32-to-i64 Array<Int> runtime owner, not the String element owner.
- Reborrow/member forwarding uses admitted routine and record-member identity.
  It may project an existing address but cannot create mutation permission or
  a copied owning array. Missing ABI/projection and wrong copy-out carriage
  remain distinct refusal controls. The final integration gate is still the
  original assignment-projection program after reduced controls pass.

### Reached signed array-literal operand (same reduced gate)

- Candidate `F1FAE84E` / source graph `23C85437` builds through the Pergyra seed
  and matches every recorded input (`51c585`, `8d9868`). The original seven-line
  Count source executes public LLVM and projects direct C/LLVM (`09a25e`).
  Native full-driver emission/compilation passes (`241f17`). The expanded gate
  now reaches `[1, -2, 2147483647]` and rejects `stage=literal`, node 7/row 7
  (`8d9868`), evidence `readonly-array-int.NGhh00/`. No compiler job remains live.
- Objective/owner: the existing populated Array<Int> literal operand owner
  must admit the persisted integer-literal -> typed Negate -> array-element
  chain. Preserve its exact source-child/spine identity, scalar Int domain,
  no binding/call identity, and ordered use prefix. Normalize through the
  existing scalar Negate kind and C/LLVM execution, not the expression's
  display text or a replacement positive-only fixture.
- Representation: the existing literal operand receipt carries operator arity
  (zero/direct operand, unary Negate, binary Subtract) instead of a binary-only
  boolean. No new owner module, syntax, arithmetic domain, or alternate literal
  decoder. Last consumers remain the shared expression arena and the existing
  C/LLVM array materializers; old binary-only residue is forbidden.
- Gates: keep negative elements and the maximum stored Int in the readonly
  fixture and actual hash-owner fixture. Mutated negate children/operator kind/
  binding identity and forged readonly mutation must reject. Existing binary
  literal/local/call operand gates remain regressions. Rebuild before rerunning
  the reduced matrix; do not yet repeat the original large integration.

### Reached readonly source-boundary objective card

- Evidence: Pergyra-seed `E909C73A` / graph `18F9B204` builds and matches its
  source inputs (`158d3c`, `a6bd2c`). The readonly gate executes eight values
  through public/direct C/LLVM and refuses all 28 MIR mutations, then fails on
  public source `ArrayPush` (`8fafd6`). Evidence is
  `.tmp/self_hosted/readonly-array-int.XyjMGu/`. A bounded follow-up records
  public acceptance of index write, borrowed-array return and ref-to-inout
  forwarding; native rejects all four forms (`248d8d`). No compiler job is live.
- Objective: a readonly parameter must not gain mutation or owning/copy-out
  permission before MIR/code publication. Keep the already passing reads,
  reborrows, member projections, literal ordering and physical ABI controls.
- Priority: admitted parameter/place identity, explicit boundary failure,
  common semantic ownership, preserved diagnostic identity, then patch size.
- Owners: `collection_mutation_policy_owner.pgy` owns direct/index mutation
  policy and currently tests only `default_param`; it must also reject
  `ref_param`. Existing expression-place, named-value-boundary and statement
  owners must be inspected for forwarding/return, without treating those
  distinct boundaries as collection mutation.
- Last consumers: source semantic verdict and its existing diagnostic relay;
  C/LLVM may consume only admitted facts. No native recheck, codegen-only
  refusal, borrowed-array copy, or mode/name reconstruction in the backend.
- Gates: preserve current RED artifacts; collect every source-negative result
  before failing. Native/public mutation, return and forwarding refusals must
  be distinguished from unrelated target rejection. Existing default-parameter
  diagnostic bytes and legal ref/inout reads remain controls. Full readonly
  and actual hash gates precede any large assignment rerun.
- The expanded actual hash gate separately reaches public LLVM builtin-call
  admission at node 8 / row 4485 (`a1159a`), after native/public C values pass.
  Evidence is `.tmp/self_hosted/cfg-identity-digest.FNYw5V/`; its direct-MIR
  phase has not run. Do not open a second parallel implementation lane.
- Implementation scope: the existing named-value boundary owner now joins
  admitted expression places and typed return-statement facts. It follows only
  decreasing member edges to a readonly root and rejects owned-sequence
  return/default/inout/own boundaries; ref forwarding remains admitted. This
  is not an alias/dataflow summary and does not claim local-alias closure.
  One Pergyra diagnostic code maps to the existing native borrow-escape
  code/cause/fix identity. No statement AST rescan or backend recheck is added.

### Readonly boundary checkpoint

Final Pergyra-seed `55445D18` / graph `b54c5081` builds, matches 2,239 inputs
and passes readonly eight-value runtime, 28 MIR refusals and 12 source refusals
on public/direct C/LLVM (`dfb05a`). Native full-driver emission/compilation,
five String/value/copy-out/record/diagnostic regressions and bounded bootstrap
source-C/MIR-producer/common-MIR-consumer comparisons pass (`0a38ce`, `3751b8`,
`6101af`, `9a24ed`). All builds/gates are terminal; shared binaries are unchanged.
The old no-op parameter mutations incorrectly treated now-supported readonly
carriage as invalid. They now omit its required fact; the mutating-readonly
negative remains in the executable gate. Previous results are hash-preserved.
General alias/dataflow closure and whole-driver fixed point remain unclaimed.

## Next ToString(Long) projection objective card

- Production entrypoint: the actual hash-owner source through public LLVM,
  not a new compiler feature track. MIR row 4485 / graph node 8 reaches the
  ToString argument around `DirectMirCfgHashString(0L, "")`.
- Falsifier: the two-line `long_to_string.pgy` under
  `.tmp/self_hosted/readonly_source_boundary_20260906/` prints 17 in public C
  and rejects LLVM at builtin-call node 4 / row 1 (`fb13f7`). Its source and
  logs are preserved. The actual hash's verified MIR is beside it.
- The final 55445D18 candidate reproduces the same C output / LLVM refusal
  (`e07950`); its logs are in `readonly_source_receipt_20260906/`.
- Objective/priority: retain the admitted Long operand and signed width through
  the existing conversion ABI; then reject missing/mismatched operand/ABI facts;
  then representation size. No hash algorithm or positive fixture change.
- Owners: `direct_mir_scalar_program_builtin_signature_projection_owner.pgy`
  joins the semantic builtin row and currently forces the integer projection's
  argument to Int; `direct_mir_scalar_program_to_string_expression_readiness_owner.pgy`
  likewise requires Int. Inspect their existing expression identity and the
  runtime string conversion fact before choosing a shared or distinct kind.
- Last consumers: existing C/LLVM ToString expression and string-runtime
  requirement owners. LLVM already requests an i64 argument; verify the
  canonical runtime's signed conversion, not just the backend spelling.
- Forbidden: truncating Long to Int, text-based type recovery, another builtin
  registry, a backend-local overload choice, native fallback or a big rerun
  while the two-line reduction is red.
- Gates: native/public/direct C/LLVM on Long results including values outside
  stored Int range and signed boundaries; preserve Int/Bool/String conversion
  controls and malformed operand/runtime identity refusals. Then execute the
  actual nine-value hash fixture on all paths before the original integration.
- Representation decision: retain normalized expression identity 14 and name
  its owner `DirectMirScalarProgramExprToStringSignedInteger`. Int and Long
  retain distinct admitted operand types; both use the existing signed-64
  conversion ABI. C materialization takes `long long` with `%lld`; LLVM takes
  `i64` with the same format. Both allocate 24 bytes, sufficient for a signed
  64-bit decimal value and terminator. No second kind, registry row or backend
  overload decision is needed. String/Bool keep their separate existing kinds.
- The canonical builtin row remains generic; call admission specializes its
  parameter fact to the exact supported operand type. Prefix validation must
  consume that fact rather than override it to Int. Missing/unsupported types
  reject; expression readiness checks the signed-integer domain and edge shape.
- Implementation is starting under the primary's sole edit ownership. No
  parallel implementation lane, installed binary change or status promotion.
- The first eleven-value control exposed a separate native literal defect:
  source `9223372036854775807L` became `-9223372036854775808LL` before conversion
  (`ba99bc`, `460e54`). The original source is hash-preserved as
  `.tmp/self_hosted/signed_integer_to_string_20260906/native_long_literal_witness.pgy`;
  native C log/output remain in `signed-integer-tostr.BzPYz3/`. The conversion
  fixture now constructs the same maximum through exact, non-overflowing Long
  arithmetic and minimum as `0L - upper - 1L`. Expected values are unchanged.
  This separates the reached conversion owner from the still-open native
  large-literal producer defect; it does not claim literal semantics closure.

### Signed conversion checkpoint and reached hash read boundary

- Isolated Pergyra-seed `2A712317` / source graph `54078130` builds and matches
  all 2,239 inputs (`eac179`, `6cda70`). Native full-driver C preflight passes
  with zero errors/four existing warnings (`4b0e88`). Signed conversion passes
  eleven values on native/public/direct C/LLVM, display-only identity and 12
  explicit refusals (`3b30e7`, `signed-integer-tostr.7vxMxP/`). Existing readonly
  eight-value/28-MIR/12-source checks remain green (`aaaf4d`).
- The unchanged actual hash source now fails earlier at public C semantic
  admission: `borrow_boundary_escape`, default Array<String> (`42e327`,
  `cfg-identity-digest.XMzrQC/`). This is not a remaining Long conversion error.
  Inspecting its preserved typed MIR finds three exact call edges (`b3eda7`):
  routine-index structure -> receiver row's source-ID array; routine-local
  type lookup -> match binding name/type arrays.
- Objective/priority: preserve readonly provenance through these existing
  read-only queries, then retain identity/ambiguity rejection and runtime
  parity. Do not copy the index arrays or relax the source escape rule.
- Owners: `program_routine_receiver_identity_owner.pgy` owns receiver row and
  unique-source-ID reads; `match_binding_local_fact_owner.pgy` owns the bounded
  name/type lookup. The exact last consumers only compare/read String elements
  and return Bool/String, never retain, mutate or transfer array storage.
- Delta: mark the receiver row's source-ID parameter and its uniqueness callee
  `ref`, and both match lookup arrays `ref`. No new owner, fallback or policy.
- Gate: retain the actual hash fixture and nine expected values; execute its
  native/public/direct targets with a fresh source-matched seed build. Keep
  readonly mutation/escape negatives and Long width controls. Only then rerun
  the original assignment integration. Primary remains sole implementation owner.

## Next UTF-8 literal projection objective card

- Checkpoint: F1A7B721 / graph 02ff15d5 builds through the Pergyra seed and
  native full-driver preflight (`6a08be`, `f33a27`). All 2,239 inputs match after
  generation (`2cd637`). Signed conversion and readonly regressions pass
  (`f09af8`, `7d85e2`); bounded bootstrap passes (`70049f`), not a full fixed point.
- Production entrypoint: unchanged actual hash source through public LLVM.
  C/native C execute all nine expected hashes, then LLVM rejects UTF-8 string
  node 6 / row 4489 (`9156d2`). The old Long and readonly call boundaries have
  been passed. Direct-MIR hash projection has not yet been reached by the gate.
- Falsifier: two-line Echo(String)/Log in
  `.tmp/self_hosted/hash_readonly_consumers_20260906/utf8_string_literal.pgy`.
  Public C prints `한글🙂`; public LLVM rejects expression-kind node 2 / row 1
  (`b8d985`). Logs and the exact-source candidate remain beside it.
- Objective/priority: retain canonical UTF-8 bytes and typed String identity,
  explicit unsupported/invalid-literal rejection, then one bounded byte walk
  and representation size. Do not redefine character/codepoint semantics.
- Owners to inspect: `direct_mir_scalar_program_string_literal_fact_owner.pgy`
  owns persisted spelling admission/decoding; expression_fact's
  `DirectMirScalarProgramLiteralEmbeddable` owns admitted payload validity;
  `direct_mir_scalar_program_llvm_string_literal_owner.pgy` owns LLVM byte
  spelling. All currently restrict non-ASCII bytes. Existing source/JSON text
  and UTF-8 contracts must be checked before choosing the precise rule.
- Last consumers: typed expression String fact and C/LLVM literal/global
  materialization. No backend type recovery, name whitelist or ASCII replacement.
- Forbidden: relaxing only admission while LLVM byte emission still refuses,
  weakening the hash/expected UTF-8 output, a second string decoder, repeated
  full-length scans per byte, native fallback or a full integration rerun now.
- Gates: the reduction plus ASCII/escape and multibyte controls, byte-exact
  C/LLVM output and explicit invalid-input diagnostics; then unchanged actual
  nine-value hash gate and original assignment integration within 1,800 seconds.
- Status: diagnostic evidence only; no UTF-8 implementation yet. Primary sole
  owner, no parallel lease or compiler job; no new status/substitution claim.
- Implementation decision after contract inspection: docs/110 already owns
  UTF-8 payload preservation, byte-length and normalization-blind equality.
  This is a missing projection, not a new Unicode text model. Preserve the
  existing literal delimiters/five escapes and control-byte rejection; permit
  opaque non-ASCII payload bytes through spelling/payload admission. Do not
  claim a new Unicode scalar validator, normalization or mixed-encoding support.
  LLVM renders those bytes as two-digit hexadecimal escapes, keeping existing
  ASCII/control spellings and byte-counted globals. Its current per-byte Concat
  accumulation is replaced by the existing TextBuilder primitive in the same
  byte-emission owner; no new decoder, owner, cache or schema.

## Reached value-array readonly reborrow objective card

- Base remains `5b97f2e10ffa7ecf9cfe932829a83ffffaa3ba12`, with the primary's
  existing dirty integration candidate. UTF-8 now passes native/public/direct
  C/LLVM, byte-length/normalization controls and 14 refusals on C100CF6E
  (`c0c6f3`). Its 2,239 source inputs match; native and Pergyra-seed builds pass.
- Exact reached caller: the actual hash fixture reaches routine 146,
  `CaptureMirProgramRoutineReceiverIdentity`, value parameter 5
  `prior_source_syntax_ids: Array<String>`, forwarding to readonly parameter 0
  of `MirProgramRoutineSourceSyntaxIdentityUniqueExcept` (`2a5a79`). The shared
  reborrow policy permits the equivalent Int value parameter but not String;
  LLVM only materializes an addressable frame slot for the Int value case.
- Objective/priority: preserve value-parameter semantics and call-duration
  readonly access, consume existing ABI evidence, delete the Int-only storage
  path, then bounded code/output size. Do not change the caller signature to
  ref merely to make this legal value-to-ref source form compile.
- Fact owner: the existing array readonly reborrow policy retains exact type,
  routine and parameter identity. The one array value-parameter storage owner
  consumes admitted Int/String projections and emits one entry-frame header
  slot per value parameter. This is not an element copy or ownership transfer.
- Last consumer: the existing LLVM readonly call argument takes that local
  slot's address. C already takes its value parameter's address. No mutation,
  copy-out, cleanup authority or escaping borrow is added.
- Forbidden: admission-only relaxation, per-call/loop stack allocation,
  invented ABI alignment, a second Int-only storage function, owner-handle
  coercion, native fallback or weakened expected hash values.
- Independent scope: primary alone edits this reached boundary, existing
  readonly fixture/gate and structural inventory. No parallel lane. Static
  budget 60 seconds, focused gate 300 seconds; builds/integration 1,800 seconds.
- Gate: old C100CF6E must reject the reduced String value-to-ref fixture;
  rebuilt native/public/direct C/LLVM must preserve its caller contents and
  the existing Int, ref, inout and member controls. Keep all readonly/ABI
  refusals. The actual nine-value hash gate is the integration boundary before
  the original assignment probe. Status: implementation candidate, not closure.
- First compile attempt is RED, not progress evidence: native `81315` rejected
  a nonexistent `TextBuilderDestroy` call (`e54e74`); Pergyra-seed `78371` also
  rejected the graph. The source preimage is hash-preserved in
  `array_value_reborrow_20260906/array_value_storage.invalid_lifecycle.pgy`.
  Existing TextBuilderFinish consumes its builder; the matching established
  cleanup is AllocatorDestroy on the result allocator. No new lifecycle API.

## Reached Intent participant read-query boundary

- Current source-matched checkpoint: 87DCF196 / graph 0E4A3D80 passes native
  and Pergyra-seed builds, readonly eleven-value/40-refusal controls, UTF-8 and
  Long regressions, actual nine-value hash parity and bounded bootstrap
  (`dc0e04`, `553606`, `94c752`, `67b5de`, `e64a5b`, `64b8fd`, `e7b25c`).
  No whole-driver fixed point, installation or new remote CI is claimed.
- Original assignment integration `11117` is terminal RED at public C source
  admission (`ae7327`), before LLVM: borrow_boundary_escape, default,
  Array<String>. Verified MIR production independently refuses (`e95f9b`).
- Read-only native MIR diagnosis names six String edges from three callers
  into `SemanticAstIntentActionCallFromText` parameters 1 and 2 (`3a0785`).
  Native MIR SHA is 79D293E7EF1EACB9842608885BA10EF776317259DC2BB42AC08290041450B035;
  its producer passed with one unreachable-statement warning (`b2f084`). This
  artifact diagnoses the source; it is not a replacement for public admission.
- Existing fact owner: SemanticAstIntentSignatureFacts owns participant names,
  types, value flags and row ranges. Callers are StepActorFromFacts,
  TransitionFactsFromArtifact and ExpressionSeedOutcome. The existing action
  query reads those rows and returns only resolved actor/action/result facts.
  Source inspection also identifies its Array<Bool> value-flag parameter;
  fixing only the two String parameters would leave the same ownership gap.
- Objective/priority: retain one participant snapshot and readonly provenance,
  exact alias/subject-action matching and ambiguity/range rejection, then
  bounded input shape. No new ResourceCarriage calculus or general query engine.
- Proposed next delta, not implemented: consume the existing participant fact
  by ref at this read query, migrating all three callers together. Compare this
  with three ref array parameters against actual consumers before editing;
  do not silently restore a by-value compatibility path or copy arrays.
- Last legitimate consumer: the existing action-query row/signature resolution
  in ast_intent_action_call_fact_owner.pgy. Keep actor/provenance distinct from
  authority; do not change Intent semantics or action selection order.
- Falsifier: original assignment source plus an import-only owner reduction in
  array_value_parameter_frame_20260906/intent_readonly_query_reduction.pgy.
  The reduction is admission evidence only, not runtime action-query dogfood.
  Add executable valid/ambiguous/missing-participant query controls when
  implementing. Keep readonly mutation/escape refusals and actual hash parity.
- Primary alone; no parallel lease. Static 60 seconds, focused 300 seconds,
  original assignment integration 1,800 seconds after the reduction passes.
  Source freeze is released, no production edit for this next boundary yet.
- Decision after current-source comparison: all three callers project the
  same names/types/value flags and the same intent-indexed range. The existing
  query will take ref SemanticAstIntentSignatureFacts plus intent_index, not
  three independently supplied ref arrays/range scalars. It derives only that
  bounded slice, rejects absent/invalid slice facts and preserves current
  actor matching order. No full artifact rescan or readiness replay per query.
  This is one existing owner-consumer seam, not a new readonly-array type rung.
  Primary begins the three-file implementation; prior goal turn was progress.
- Current implementation removes all three value-array inputs and independently
  supplied start/count scalars. The original owner derives the bounded range
  from the borrowed participant fact and rejects bad snapshot/index/column
  bounds before scanning. The same alias/action rules and actor order remain.
  Actual eighteen-result native/public C/LLVM query control passes prebuild;
  the old import-only reduction now emits public C (`a7c580`). First test setup
  rejected an unimported Die call; the fixture uses existing Log/Exit instead.
  Inventory/caps pass (`d0f961`). No interpretation of those as a full Intent
  execution proof. Source freezes for native `43479` and seed `27336` builds.
- Both first whole-driver builds are terminal RED (`6dd4be`, `c436ec`). The
  initial diagnosis covered the assignment import graph, not all production
  callers: SelfDirIntentStepFromArtifact still passed seven arguments. The
  TextBuilder diagnostics follow this first arity error; no cleanup workaround.
  Full src/tests call inventory identifies four production calls plus the new
  fixture (`c73877`). This corrects the earlier three-caller scope claim.
- The DIR assembly owner already iterates the same admitted signature fact
  with intent_index and copies its rows into derived DIR participant columns
  while adding declaration indices (`15ceaa`). Carry that existing index to
  the step consumer and use the semantic fact for the action query. Keep DIR
  columns for their legitimate placement/transfer/provenance consumers; do not
  infer a semantic range from copied rows or rescan the AST for a parent index.
  Two existing DIR files join this same migration. No replacement fallback.
- Current implementation verification: isolated driver C681F4BF, source
  manifest 20854311, native full-driver PASS, seed log/receipt/hash agreement,
  all 2,240 source hashes matching. The eighteen query/actor-order outputs pass
  native/public C/LLVM (`be8c2e`). Bounded existing Checkout and canonical
  multi-intent execution slices pass (`d02e5c`, `2bc1af`), including non-first
  intent indices and success/failure. Readonly eleven-output/40-refusal and
  actual nine-value hash gates pass (`239426`, `04ab06`); bounded bootstrap
  passes (`f5210d`), not full fixed point. No new semantic authority or status.
- Original assignment integration `92648` is running once under 1,800 seconds;
  keep source frozen and observe the same job. Prior 270 files/40,441,665 bytes
  are hash-preserved. Evidence is in intent_participant_dir_carriage_20260906/.
  Next falsifier remains this original program, not a new parallel cleanup
  track. Primary alone; no shared install, staged files, commit/push or CI write.
- That same run now passes C compilation, all five expected output lines and
  seven missing-fact refusals (`a8953e`). The old Intent readonly-to-value
  source refusal is crossed. LLVM producer PID 92176 remains live; the next
  falsifier is its result, not another full C compilation. Keep the one run.
