# Post-formatter self-host parallel audits — 2026-08-28

Status: `AUDIT COMPLETE`
Base revision: `97d54a649cfaa2d38022d5d4e866292efb013e03` plus the primary
task's dirty public-formatter substitution lease.

This directive coordinates observations only. It owns no compiler fact,
progress number, registry state, or successor implementation rung.

## Shared objective card

- Objective: select the next production C-to-Pergyra self-host substitution
  after the public formatter is published, with special attention to module
  composition and bootstrap entrypoints.
- Priority: real production bypass replacement, stable identity, one existing
  or explicitly proposed fact owner, old-path deletion, fail-closed negative
  gate, then patch size.
- Current exclusive owner boundary: the primary task owns all formatter,
  lexer-fact, installed-format request, SoT-registry, proof projection,
  progress-document, integration, Git, and CI edits.
- Forbidden fallback: counting registry rows, files, fixtures, tests, agent
  output, or generated artifacts as substitution progress; proposing dual-read
  compatibility; reopening an unrelated query/cache/performance track.
- Integration owner: the primary task. The current integration gate is
  `tests/self_hosted/parity/public_fmt_installed_self_host_owner.sh`; any
  successor must name one production entrypoint and a new focused executable
  or negative gate before implementation begins.

## Independent read-only scopes

### Module-composition audit

Inspect `source.module_graph`, `src/compiler/module_loader.*`, import/module
owners under `src/self_hosted/`, and their reached public/installed paths.
Report the exact native bypass, existing Pergyra facts, last consumer, missing
fact, and smallest falsifying fixture for a possible self-host substitution.

### Open-authority priority audit

Inspect the current SoT registry and live consumers. Rank no more than three
`BRIDGE`/`ACTIVE` seams that are already reached by a production self-host
entrypoint. Reject candidates that are owner-only cleanup or require a general
query/cache architecture. Report evidence and why each candidate is or is not
independent of module composition.

### Bootstrap-bypass audit

Inspect the active self-host scorecards, installed driver composition root,
native launcher/product commands, and focused parity gates. Identify the next
direct C-owned public compiler path after formatter, including entrypoint,
existing Pergyra owner, last C consumer, forbidden fallback, and one bounded
gate. Do not infer readiness from surface syntax or parser-only reachability.

## Edit and command boundaries

- All three scopes are read-only. Do not edit, stage, commit, push, or run a
  full matrix.
- Do not inspect, edit, or stage `docs/compiler_architectures/` or
  `pgy-80135c2c/`.
- Allowed commands are `git status`, `git diff --name-only`, `rg`, and bounded
  source/document reads. Budget is five minutes per audit; no build is needed.
- Return observations and proposals to the primary task. A report is not an
  implementation authorization or completion claim.

## Read-only result

- Module composition is not a BRIDGE-reduction successor.
  `source.module_graph` is already `CLOSED`; installed Pergyra parsing already
  owns relative-import normalization, deduplication, recursive composition,
  and top-level source-module facts. Its nearest open family is the much wider
  `parser.syntax_provenance`, whose native AST identity still fans out to
  semantic, HIR, DIR, and RIR consumers. A module-only delta cannot close it.
- The default public/bootstrap/package/REPL compiler path has no remaining
  implicit `driver_run_pipeline` retry to delete. Remaining calls are explicit
  native opt-in or test oracle paths. Bare HIR/AIR/RIR requests lack a complete
  Pergyra producer, so they cannot be honest existing-BRIDGE successors.
- The only production-reached candidate that survived the strict filter is
  existing row `selfhost.intent_declaration_rows`. Direct source-C currently
  supplies `CodegenIntentExecutionViewEmpty(true)`, leaving
  `intent_emit_owner.pgy` to re-walk typed AST children, reparse step headers,
  and reconstruct step/compensation arrays beside the typed plan. The bounded
  successor must carry one complete admitted transition view, delete that
  reconstruction and absent-view fallback, and fail before artifact
  publication on missing or crossed facts.
- Candidate integration evidence is
  `tests/self_hosted/parity/intent_guard_post_compensation_execution_owner.sh`,
  extended through the real installed/public source-C entrypoint with static
  old-read bans and no-artifact mutations. This audit does not authorize that
  implementation until the active formatter lease is published and released.
