# Source-admission parity and active-document consolidation

Status: ACTIVE; primary-only implementation, no parallel edit lane.
Base HEAD/origin/main: `5b97f2e10ffa7ecf9cfe932829a83ffffaa3ba12`.
This is a coordination card, not semantic authority or an execution log.

## Current integration blocker — push CI at 3da11372

Run `34280162606` reached a green codegen bootstrap, sanitizers, native tests
and integrated driver gen2 == gen3. Repair its remaining registered-case and
generated language-word inventories through their existing owners. The active
executable boundary is the following source/MIR-to-LLVM Intent gate chain.
The existing declaration index owns wire kind separately from nominal kind;
composite/nested route consumers must not miss canonical subject/zone rows.
The legacy Intent plan owns the admitted completion graph, and LLVM is its
last consumer. Carry and evaluate that predicate after the step expectation;
do not replace a nonconstant success clause with `true` or weaken the fixture.
Keep claimed malformed inputs fail-closed, with no scalar/native fallback.
Integration gate: legacy, composite and nested Intent C/LLVM execution and
their negative cases, then the source-action and installed CLI gates already
named by the full-bootstrap CI job. No parallel implementation lane is open.

The preparation gate also reached native IO-summary SSA use reconstruction:
RIR IO summaries keep their effect evidence, but the executable DEF/STMT owns
operand reads at its lexical position. Never resolve a summary's display path
as an entry `.0` local or relax LLVM's exact-storage guard. The MIR lexical
identity unit gate and `ssa_loop_io_operand` falsify this boundary.

The remaining preparation contracts reached expression references sharing an
enum declaration ID and typed lookup absence. Expression collection retains
exact (declaration, spelling) pairs; the local SSA owner still rejects a wrong
local identity. Existing capability, Future, generic-call and domain-projection
owners return Option at their reached lookup boundaries. Missing rows must
still refuse admission or retain the existing nonconstant-flow alternative,
never become zero, a guessed row or a native retry. Their executable admission
gates and a fresh Pergyra-built installed driver are the integration boundary.
The likeness gate distinguishes exact schema-absence checks from control-flow
sentinels; no whole-file sentinel exemption or raised cap is authorized.

### Previous CI repair boundaries — lookup only

The user reopened run `34251704201` on 2026-09-09. Repair its reached
integration failures before extending Intent GraphPlan execution. Primary only.
Objective: restore fresh seed construction and the existing native ABI/LLVM
gates without weakening source admission, fact validation or negative checks.
Priority: producer/import correctness, exact binding carriage, executable
regressions, then stale source-pin repair. Existing parser/lowering/semantic
binding and LLVM storage owners remain authoritative; seed construction,
MIR validation and target emission are their last reached consumers.
Forbidden: guessed binding modes, spelling-based SSA lookup, native fallback,
skipping failed jobs, or increasing limits to obtain green.
Focused falsifiers are the codegen bootstrap seed, the nominal generic C
specialization test, `world_embedded_projection_abi`, and
`positive_own_remote_transfer`. Integration is the existing push workflow;
passing a local candidate does not establish remote CI success.

Reached seed sub-boundary: graph-owned call argument types must not depend on
the callee returning a scalar or on scalar-only formal parameters. Collection
field receivers reuse the admitted expression graph and nominal field owner,
then the existing C expression/type projection and binding owner. No dotted
spelling is a local-binding identity. Last consumers are statement admission
and C mutation emission; `codegen_call_argument_graph.sh` owns valid execution
and wrong-argument pre-publication refusals. Keep missing graph/type/binding
failures explicit. Bootstrap is the integration gate, not source-pin success.
The next reached boundary is borrowed callable-table carriage from Intent
predicate admission through the expression verdict and its query callees.
Only readonly name/return/parameter table inputs move to `ref`; mutable table
builders retain `inout`. Signature type-expression readers also borrow their
input rows and generic-name tuples. No array copy grants ownership here.
Field mutation retains the root parameter's readonly mode in both native and
graph admission. Statement intrinsics and ordinary collection calls share the
graph receiver-mutation owner; projecting a member never grants mutation.
The same focused gate rejects readonly field writes and borrowed-array escapes
before artifact publication. These are also checks of the native comparator,
not assumptions that native acceptance defines the intended language.
The driver then reaches declaration scheduling for a nominal-record array field.
Keep the existing typed Intent step plan and nominal-array layout/runtime owners;
do not flatten the plan or invent an unverified ABI spelling to bypass the error.
The semantic nominal-constructor lookup admits array element identity once;
the existing nominal-array usage fact feeds both declaration and runtime emission.
Declaration scheduling emits named element forwards and complete pointer/len/cap
descriptors before value fields, then completes elements before runtime bodies.
Direct by-value dependencies still require completed declarations. Last consumers
are the C field layout and collection runtime owners. Falsifiers: array-valued
record fields (including recursive arrays) execute; direct value cycles refuse
before C publication. Driver generation is the integration gate.
The native comparator exposed the same boundary: its existing array macro now
separates descriptor/prototype declaration from value-taking implementation.
The specialization registry emits the latter into the existing late file-scope
declarations, never into a partially emitted generic function body. No runtime
layout or collection semantics change, and builtin combined expansion remains.
After this boundary, identity-cell definition routing must copy the immutable
store receipt's use prefix into its local consuming cursor, as the neighboring
member/indexed routes do. Never pass an immutable receipt field as `inout` or
relax the C argument owner to make that source compile.
The verified-artifact route also lowers those admitted array field mutations to
MIR. Its primary LocalRef belongs to the lexical root selected from the carried
member graph; the existing expression graph retains the projections. Do not
look up a dotted spelling as a local or drop the graph to make verification pass.
The full zone-sync gate's codegen build (not a prebuilt bypass) is the falsifier.
The final structural line-cap batch is a remaining integration obligation, not
permission to raise caps or split semantic responsibilities indiscriminately.
At that boundary, preserve executable bodies and existing fact identities first.
Separate lifetime retirement from text assembly, Intent binding/canonical-plan
projection from ordinary signature/instruction sealing, and ABI receipt keys,
physical-layout checks and expression emission from their respective consumers.
The manifest names these physical responsibilities; it does not add semantic
fact families. Delete each relocated definition from its former module, migrate
imports and source pins, and ratchet duplicate definitions. The structural
component gate plus fresh bootstrap/driver parity own integration acceptance.
The reached inferred-generic negative also requires each template return's
indexed ABI type to agree with its unspecialized header before actual-type
substitution. `mir_lower/generic_call_occurrence_index_owner.pgy` seals that
cross-edge once per template; instance signature projection is the last consumer.
Forbidden: accepting a crossed T/Int source receipt because this instance binds
T=Int, or weakening the existing return-type mutation. The same gate retains
a generic function with a concrete return and two different actual tuples.
Discarded AST_CALL statements also reuse the existing exact ABI-absence owner
before expression admission; null type, zero layout ID, false required bit and
null layout cannot be replaced by a stray value receipt. The statement owner
is the last consumer. Existing output-ABI mutations remain required refusals.
The installed role-override rung consumes declaration storage kind `class`
and semantic nominal kind `subject` as distinct admitted facts. Its identity
owner must require that exact pair, not reinterpret one as the other. Existing
role C/LLVM execution is the positive gate; crossed wire/nominal kinds refuse.

## Objective and priority

The public compiler and native checker must implement the same canonical
language. Invalid-source refusal, valid admission and actual execution are
separate obligations. Native is a comparator, not an infallible oracle.
Owner counts, gen2/gen3 equality and a collector's exit status cannot replace
these checks. Freeze new syntax and unrelated runtime/performance lanes.

Priority: stable semantic identity, owner-directed facts, missing-fact refusal,
consumer migration and old-path negative gates; then performance and patch size.
Source/binary hashes and gate outcomes belong only in the
[single snapshot](../current_work_handoff.md). Historical implementation detail
is not a successor work queue.

## Reached capability boundary

- Production entrypoint: `driver_bootstrap_main.pgy` source modes, through common
  semantic body admission to MIR/C publication.
- Pergyra fact owner: `semantic/ast_capability_fact_owner.pgy`; call projection and
  instantiation are internal responsibilities of that family, not new SoTs.
- Last consumers: common body verdict/readiness and the inspection manifest.
- Deleted path: manifest-only inference. The manifest reuses `body.capabilities`.
- Forbidden fallback: native preflight, unknown-as-pure, name rejoin, missing-ID
  zero masks, a second capability vocabulary or unioning unrelated invocations.
- Focused gates: `callable_capability_admission.sh` and
  `intent_capability_admission.sh`; integration: `source_admission_parity.sh`.

Function/action and Intent declaration identities remain distinct. Admitted
expression-surface facts retain purpose attribution; the old downstream Intent
ancestry query remains deleted. The capability graph substitutes callable formal
IDs per ordered actual tuple. Unused callbacks contribute no use; intermediate
bounds still apply. Unsaturated templates carry explicit deferred use; a closed
invocation cannot retain an unresolved target.

## Native comparator repair — implemented, bounded

Native lexical call targets now survive semantic/C/LLVM lowering by stable
binding identity. Backend registry presence is storage/signature evidence, not
permission to choose a later local or reopen a builtin spelling. The previous
SSA use-edge omission for ASSIGN/LOOP_INIT is also repaired; it must not be
hidden by disabling DCE or rewriting the Pergyra source.

`callable_capability_inference.c` collects typed equations while bodies are
checked once. Declaration/formal/binding identities and ordered actual tuples
own invocation identity. The program seal solves recursive dependencies and
checks declared bounds; the old inline scalar-mask bound check is removed.
Single-provenance returned callables and aliases retain identity. Value
constructors are distinguished from body invocation. Resolved methods, Intent
bodies and registered event handlers contribute dependencies. Unsupported
mutable/mixed-return targets fail at a closed use, not as a guessed pure mask.

The existing capture owner rejects captured callable storage; scalar copy
captures do not change capability identity. Do not add lexical-environment
instances for them: recursion can recreate the same local lambda indefinitely.
These equations belong to the semantic scratch arena and die with its analysis;
they are not another published artifact or top-level fact family.

Native unit controls exercise forward inference, returned/formal callable
isolation, aliases, unused/invoked lambdas/events, method authority, missing
provenance, constructor callbacks and finite recursive-lambda reuse. Existing
paired controls remain the canonical gate. Passing MIR admission does not
close the separately failing nested-callable execution ABI.

## Reached effect boundary and next falsifier

`authority_effect/clock_wrong_effect.pgy` grants clock but declares local-only
effects. Both paths now refuse its Now call. Native forward/formal-call effect
omissions are also repaired through the existing invocation graph, with direct
effects kept separate from partial branch/parallel diagnostic provenance.
The same graph transports independent cap/effect masks and ordered actual
identities; do not add another possible-callee analysis or convert cap bits.

The public producer reuses `ast_action_contract_fact_owner.pgy` effect ranges;
`ast_body_type_bundle_owner.pgy` consumes its verdict before publication.
`builtin_effect_registry.def` owns fixed call effects for native consumers and
the generated Pergyra projection. That family stays ACTIVE. Canonical details
belong in `docs/semantics/15_capability_sandbox.md`, not this directive.

`authority_effect/effect_unclassified_call.pgy` is now a positive regression:
ArrayLength under a local bound must be admitted and execute identically.
Classification comes from the legitimate builtin/operand owner; do not remove
the source annotation, guess purity, scan names again, or add native preflight.
`known_call_effects` remains a lower bound, not whole-language purity evidence.
Resource, statement, suspension and general callback-builtin effects stay open.

Focused gates: `effect_admission.sh` and `effect_call_fact_owner.sh`.
Integration: `source_admission_parity.sh`. Only this executable rung is active.

The current classification delta covers the existing native scalar/string/math,
non-callback collection and HashMap dispatch families, not a single ArrayLength
exception. Their intrinsic effect policy moves behind the same fixed registry;
resolved dispatch must require a row. Child-expression effects remain separate.
ArrayMap/ArrayFilter invoke callbacks and cannot become fixed-local rows.
The registry gate must compare complete reached dispatch inventories and reject
missing rows; source gates must include effectful arguments and lexical builtin
shadowing as falsifiers of an accidental zero-effect shortcut.

The HashMap signature gap is now implemented at `builtin_signature_owner.pgy`
and its existing contextual/graph type consumers, ending at the common body
verdict. Seed placeholders cannot authorize publication: constructor context,
concrete key/value types and argument verdicts are required. A fixed effect row
still does not imply source or backend support. Keep the positive
`authority_effect/effect_map_local_valid.pgy` source and both backend outcomes
as obligations. Separate scalar and collection execution failures remain
visible in the same gate, with no expected-failure allowance. Exact results
belong in the snapshot, not another progress log.

HashMap implementation card: extend the existing collection-call protocol for
constructor, receiver, key/value argument positions and result shape. Native
`type_checker_collection_policy.c` remains the stable-key policy owner; any
Pergyra key predicate must be a checked mechanical projection, not a second
literal policy. The semantic builtin signature owner specializes the protocol
with concrete HashMap<K,V> facts. Existing graph argument/type verdicts consume
them before common body admission. Preserve lexical callable identity and
collection mutation modes. Negative gates cover wrong receiver/key/value,
arity, unsupported keys, nested calls and missing contextual types. The final
gate still requires native/public C/LLVM execution, including MapGet/MapKeys
result carriage; admission alone cannot close this rung.

The next reached gap is execution carriage, not looser admission. Public C
stops in `codegen/emission/stmt_emit.pgy`; C value/call layout must consume
concrete semantic types through the existing ABI boundary. The narrow
`CollectionListRuntimeHashMapCValueType` is a List-element projection, not a
general HashMap ABI. Public LLVM falls through the unclaimed program routes
in `direct_mir_backend_projection_owner.pgy` to the Int-only block projection
in `direct_mir_scalar_graph_admission_owner.pgy`. Do not weaken that Int guard
or add a fixture-shaped route: storage, key operations and result carriage
need owned facts before the target emitters. Preserve canonical runtime ABI
ownership and the same semantic identity across C/LLVM. The integration gate
is `hashmap_admission.sh`, with all supported scalar keys, replacement/removal,
sorted owned key snapshots and callable-shadowing controls; keep the broader
`effect_admission.sh` and `source_admission_parity.sh` failures visible.

C carriage implementation boundary: project key ABI infixes from the existing
native `codegen_hashmap_key_policy.c` table; runtime headers continue to own
map storage and operations. The existing semantic type-usage view selects
specializations and header requirements once. ABI layout, call emission and
key-snapshot descriptor conversion consume that projection. Delete the old
List-only HashMap C-type guess. Preserve declared/formal callable identity and
sequence receiver/key/value once in source order. Missing type/ABI facts must
refuse; do not substitute a String/Int layout or copy the runtime implementation.

LLVM carriage implementation boundary: the existing scalar-program route and
typed graph planner consume the same concrete collection signature, with one
HashMap operation family. Runtime key ABI and raw storage remain projections
of the native runtime contract. The last consumers are local storage, ordered
call emission and owned key-snapshot conversion; no new program-shape route.
Missing receiver identity, key/value ABI or result facts refuse before emission.
The falsifiers are the same four-key execution controls, a left-to-right call
with effectful key/value operands, and altered/missing normalized call facts.
Int key snapshots keep native i32 element storage: the existing Array<Int>
ABI owner widens an element to the scalar planner's i64 value on read. Do not
infer array element layout from the scalar expression representation.

## Implemented execution card — scalar and Array effect composition

HashMap source controls are now green; bounded evidence is in the snapshot.
The completed bounded implementation extends the existing scalar-program planner's
missing checked arithmetic and array transforms, plus the reached native
ArrayReverse comparator omission. No new source surface or program-shape route.

- Objective: the unchanged valid scalar/collection effect controls execute
  with the same results and failure semantics in native/public C/LLVM.
- Priority: canonical operation identity, runtime ABI, ordered operands and
  mutation semantics; then patch size. Source admission stays strict.
- Fact owners: semantic builtin signatures, normalized expression facts,
  runtime-call ABI registry and the existing Array storage ABI. CheckedAdd/Mul
  keep the native i32 checked-overflow contract. ArraySort/Reverse are storage
  mutators in the native semantic owner; reverse must not silently clone.
- Last consumers: typed expression readiness and C/LLVM operation emission.
- Forbidden fallback: ordinary wrapping addition/multiplication in place of
  checked calls, native retry, dropped array mutation, fabricated ABI defaults,
  fixture-specific dispatch, or discarded effect clauses.
- Gate: effect_admission.sh with exact scalar 4,7 and collection 2,3 outputs;
  preserve HashMap regression controls. Add composition and altered normalized
  fact controls rather than only a builtin-name source pin.

The full component structural gate exceeded its 60 s budget (confirmed inner
status 124), not an observed semantic refusal. Do not raise the allowance or
convert the unfinished run to success; inspect repeated work at integration.

## Implemented execution card — zone application admission

- Objective: refuse a rule whose participant lacks the exact zone-slot authority
  or whose effect slot names a different nominal kind, before public MIR/C.
- Priority: slot/declaration identity, existing typed owner facts, explicit
  diagnostic and no publication; then patch size.
- Owners: parser/HIR zone-rule facts, semantic nominal constructor identities
  and SemanticAstZoneAuthorityFacts. Reuse the artifact analysis boundary.
- Last consumer: the common semantic artifact/body verdict used by production
  source entrypoints; no separate manifest-only validation.
- Forbidden fallback: same-type Slot substitution, class-as-effect, absent rule
  metadata as permission, downstream AST-text reparse or native preflight.
- Falsifiers: authority_wrong_same_type_slot.pgy and effect_layer_as_class.pgy,
  with valid owner and real-effect controls. Gate: source_admission_parity.sh
  plus focused zone-rule source/fact controls. Invalid programs never execute.

Source controls 18/0 and typed fact controls 6 PASS on the candidate recorded
in the single snapshot; this is bounded admission, not all domain semantics.

## Verified admission card — generic ability constraints (execution open)

- Objective: a concrete generic call must satisfy its declared `where` bounds
  before the source program can publish MIR/C.
- Priority: formal/callable identity, canonical role implementation facts,
  common admission, diagnostic and negative gate; then patch size.
- Owners: typed generic-constraint rows in callable signatures, admitted
  generic specialization bindings, and SemanticAstRoleFacts.
- Last consumer: the common body verdict; codegen consumes admitted bindings.
- Forbidden fallback: ignoring `where`, matching an unrelated type's role,
  treating absent bounds as empty after projection, downstream source reparse,
  native preflight, or rejecting all generic calls to conceal the gap.
- Falsifier: f_where_ability_bad.pgy; controls include a real implementation,
  explicit/inferred actuals and more than one bound. Source refusal only for
  invalid programs. Preserve the zone and existing admission controls.

Native/public bound admission and binding/diagnostic controls passed. Native
LLVM now consumes the MIR actual/formal frame; public C closes transitive
specializations per caller instance. Public LLVM execution remains open. Keep
the original programs unchanged, and distinguish bound admission, call-location
carriage and concrete backend instantiation. Current numbers and hashes belong
in the single snapshot, not this directive.

## Implemented bounded admission card — world-owned zone escape

- Objective: prevent a live world-owned zone from escaping as a value/call
  argument without its explicit detached-copy boundary.
- Priority: exact world/zone field identity and expression use context;
  existing resource-carriage owner, common refusal, then patch size.
- Owners: semantic nominal fields, admitted expression graph/receiver facts,
  and zone resource-carriage verdict. Native docs/157 contract is the comparator.
- Last consumer: common body verdict before source MIR/C publication.
- Forbidden fallback: classifying only the final type, permitting Intent call
  arguments to bypass the world boundary, treating a shadowed `Clone` spelling
  as copy authority, or downstream source-text scanning.
- Falsifier: tests/cases/axis_composition/comp_world_intent/cross.pgy. Add valid
  detached-copy and ordinary world-member controls plus direct argument and
  local/return negatives as reached. Invalid programs are never executed.

No parallel implementation lane. The local callable-alias negative still fails
at earlier callable typing, not the owned world claim; its gate stays red and
unchanged. Formal callable shadowing and explicit Clone identity are covered.
Generic and previously reported backend execution gaps remain obligations.

Reached native-build blocker: the unchanged world-use owner exposes C edge
copies between disjoint same-spelled Int/Option locals. The MIR liveness owner,
not the emitter's complete entry-version map, authorizes a non-phi edge copy.
Remove dead entry-map restoration in `transpiler_mir_phi_emit.c`; preserve live
copies and real phi inputs. The small same-spelling/different-type loop control
must compile and return 6,0, then the unchanged world-use owner must build. DCE
also removes unobserved phi-only cycles by following existing value-summary
dependencies from non-phi uses; source-backed and copy-out phis remain roots. Do not
rename the source local or disable SSA/DCE to conceal this comparator defect.

## Verified publication card — typed Intent v3 source publication

- Objective: production source-to-MIR carries the existing typed Intent plan,
  tied to real routine/CFG/instruction identities, through the existing v3 seal.
- Priority: admitted transition and payload identity, owner-directed lowering,
  missing-fact refusal and exact cross-seal; then emission size/performance.
- Owners: semantic Intent transition facts and DIR step/terminal projections;
  `MirIntentExecutionPlan` owns the sealed executable transition plan.
- Last consumers: source MIR JSON publication and existing direct-MIR plan
  admission/C/LLVM consumers. Canonical instruction IDs must precede sealing.
- Forbidden fallback: a metadata-only plan pointing to invented blocks,
  dropping typed outcomes into Bool, reparsing source/AIR, native preflight,
  or a second Intent schema/digest/semantic owner.
- Falsifier: `intent/single_step_exact.pgy` in `source_admission_parity.sh`,
  followed by the existing plan cross-seal and altered/missing identity controls
  and valid source execution. Keep predecessor, compensation and terminal
  composition visible; one simple plan is not whole-Intent closure.

The original integration gate is 9/0 and the added source cross-seal/mutation
gate is 10/0 on the candidate in the snapshot. This is not universal direct
backend Intent execution; the extra native-single MIR direct-C probe remains
an explicit referenced-enum route refusal.

## Active execution card — generic specialization execution carriage

- Objective: the unchanged valid bound/forwarding/type controls run with the
  same result in native/public C and LLVM after their admission succeeds.
- Priority: actual/formal and callable identity, admitted specialization facts,
  concrete ABI substitution, explicit refusal of missing facts; then patch size.
- Owners: semantic generic specialization bindings; existing MIR specialization
  rows and backend concrete type/carriage consumers. Bounds are already admitted.
- Last consumers: nested generic C specialization and LLVM call/function ABI
  emission through existing supported program admission, not a new shape route.
- Forbidden fallback: classifying nominal actuals by LLVM storage type alone,
  forwarding an unbound formal as a concrete C type, stripping bounds, native
  retry, or a test-specific route/expected-failure allowance.
- Falsifiers: generic_bounds_valid, generic_bounds_forward_valid and
  generic_bounds_type_valid in generic_bound_admission.sh. Preserve their
  eighteen paired admission controls and exact execution results.

Public MIR now preserves the semantic call handle through production and
seals its routine/block/instruction/lane/node occurrences at canonical
instruction identity. The production backend verifies those anchors, coverage
and the callee declaration ID before dispatch. Neither source-owner
preorder, callable spelling, routine count nor an unused parameter authorizes a
binding. Missing, crossed and duplicate occurrence controls precede backend
integration. This is carriage within `mir.generic_specialization`, not another
fact family or a fixture-shaped program route.

The active falsifier is unchanged source `generic_bounds_type_valid.pgy` through
public LLVM, followed by multiple tuples, recursive forwarding and a typed
body-local copy. Concrete signature/body instantiation uses a separate instance
identity while preserving the source routine ID. Do not erase an unused
argument, forge a source ID, rewrite JSON text, add another two/three-routine
classifier, or count the metadata delta as complete generic execution. Multiple
actual tuples and recursive forwarding must share that same instantiation owner.

The reached implementation is `semantic/generic_instance_closure_owner.pgy`:
both source/reconstructed-MIR C adapters and the MIR-only occurrence adapter
use its declaration-plus-ordered-tuple closure. Symbol spelling is a checked
projection, not instance identity. Recipes are indexed once by the owning
template; a concrete caller selects a retained occurrence, and same-instance
recursion reuses its body. This replaces the separate C-only closure algorithm.
The general scalar signature/body consumer now receives a Main-first execution
inventory, exact call-occurrence bindings and concrete scalar signatures. Its
routine partition retains source IDs with distinct ordered actual-tuple keys.
Typed body locals and physical ABI receipts remain the reached falsifiers;
signature or GraphPlan probes alone do not close LLVM execution. Nominal
identity actuals must use an owned concrete receipt, never scalar defaults or
passive-record reinterpretation.

The reached native comparator seam is also concrete parameter carriage:
`Rank<Card>` currently emits a by-value C parameter while LLVM uses subject
identity. The MIR-selected actual type must reach the existing nominal
carriage classifier in parameter declarations, bindings and call arguments;
raw formal `T` cannot authorize a value copy. Preserve the no-copy identity
contract and require a direct/member/forwarding ABI falsifier before counting
the nominal execution obligation closed. Native remains a comparator, not an
infallible authority for this discrepancy.

The shared GraphPlan extension carries subject cells separately from passive
logical records. The declaration index and canonical receiver-carriage policy
own their nominal/field identities; exact absent physical-layout envelopes
authorize only compiler-private storage. The last consumers are concrete
parameter roles, fresh construction, local/argument pointer flow and C/LLVM
emission. No record-value operation may claim a subject through this extension.
Return/aggregate/collection escape remains refused until a lifetime receipt
exists. Gate: unchanged bound programs plus direct/forwarded cell reads,
multiple identities and malformed/missing declaration/ABI controls; no new
program-shape route and no parameter erasure.

The next reached source falsifier is `generic_identity_read_valid`: native
C/LLVM reads distinct cells through `T: Card`, while public semantic typing
cannot resolve the field. Native `type_system_compat.c:type_satisfies_constraint`
and the public bound verifier define a nominal bound by exact type equality,
not subtyping. A body environment may derive that equality from the enclosing
signature and the nominal declaration owner; it must retain the original
formal/signature handles and cannot invent an equality for an ability-only,
unbound or differently scoped formal. The last consumers are parameter/body
type views and existing expression field queries. Gate: the unchanged valid
source and wrong-nominal admission negative, with unbound/ability-only isolation
checked at the equality-fact boundary. Native accepts structural member use on
unbound and ability-only formals; these are positive instantiation obligations,
not invalid-source negatives. Keep the three corresponding `_valid` sources
as open public-path falsifiers until every reached instance has a field witness.

The reached C round-trip seam is routine generic constraint carriage. Objective:
preserve the declared bound through source admission, MIR and reconstructed-body
verification. Priority: exact formal identity and explicit absence, then compact
projection. Owners: semantic signature constraint rows and native MIR signature
capture; the MIR header inventory is the last projection owner for the C body
consumer. Every generic formal carries one ordered `{name, constraint}` row,
including an explicit empty constraint. Missing, crossed, duplicate or malformed
rows must fail before backend dispatch. Do not infer bounds from call actuals,
skip re-verification, or retain a source-only compatibility path. Gate: the same
seven-source execution matrix, paired source bound controls and header mutations.

The next reached identity-cell seam is source admission of subject copies and
bare subject returns. Objective: reject the same anchored-identity boundary
before public MIR publication. Priority: nominal identity and typed source
facts, stable diagnostics, negative source gate. Owners: admitted nominal
constructor kinds, initializer/binding types, signature return types and
expression roots. Last consumer: the common body verdict before MIR; backend
lifetime refusal is not a substitute. Do not treat value class/struct copies
as subject copies, infer identity from a type-name suffix, or scan source text.
Falsifiers: subject_binding_copy_rejected and subject_return_rejected, plus
fresh subject construction and class-copy positives in both pipelines.

The next executable positive is `nominal/class_value_copy_valid.pgy`: admission
and native/public C execute 7,19, but public LLVM still enters a two-instruction
nominal-literal envelope. Objective: passive value construction/copy/field writes
use the existing general logical-record GraphPlan. Priority: value versus
identity separation, declaration/field IDs and layout receipts, then deletion of
the passive literal bypass and its negative ratchet. Owners: canonical nominal
carriage policy and logical-record declaration inventory; last consumers are
the shared C/LLVM record emitters. Do not add a special class-copy route or
insert a spare routine in the fixture. The separate mutable-identity slice must
not be reinterpreted as passive records. Gate: source admission positives,
existing passive nominal literal C/LLVM mutations and identity regressions.

The reached wire seam is declaration container kind versus nominal identity.
`mir/declaration_wire_kind_owner.pgy` owns the existing pgy.mir.v1 mapping for
publication and reached value/identity declaration consumers. A `class` wire
container does not imply class value semantics: `nominal_kind` remains exact.
Runtime-bearing record receipts take precedence over compile-time declaration
erasure; no failed record may retry as declaration-free code. Preserve source
declaration/field IDs and reject noncanonical or crossed wire kinds. The old
passive literal emitters are deleted; their shape-only limits are replaced by
general execution controls, not relabeled as universal language restrictions.

Native comparator falsifier: `nominal/named_record_fields_valid.pgy` exposes
positional insertion of named class fields in LLVM (31,7,19 rather than
7,19,31). The existing class-field registry owns name-to-logical-ordinal and
logical-to-physical-slot mapping; constructor evaluation stays in source order.
Do not reorder argument evaluation, assume ordinal equals layout slot, or
reinterpret the differing result as the language contract.

The active C projection seam is named-initializer evaluation order. Objective:
each admitted field RHS executes exactly once in source order while field
identity still selects its declaration/layout slot. Priority: observable order,
owned field/type metadata, no old unsequenced path. Owners: native hosted field
view and public semantic struct-literal graph; shared direct-MIR GraphPlan
operand rows retain source order independently of declaration order. The three
C constructor projections are the last consumers. Use the existing statement-
expression convention with collision-free local names; preserve slot claims
and value/identity boundaries. No source semantic rescan or backend-dependent
ordering is permitted. Falsifier: named_record_evaluation_order_valid expects
3,1,2,1,2,3 on native/public C/LLVM; retain the pure-field, passive record and
mutable identity controls. This does not claim binary RHS ordering is closed.

The expanded native-MIR control also falsified an overstrict definition check:
native unannotated `let` carries its inferred type in the ABI/source-local
facts but has null expr1; reconstructed public source can carry a type node.
expr1 is an optional source annotation, not a second inferred-type authority.
Compare it when present; never use it as a fallback for a missing definition
ABI fact. Retain both present-but-crossed type mutations and the same valid
native/public MIR constructor execution.

The next source-admission seam is finite match coverage. Objective: refuse a
missing named enum variant before public MIR just as native admission does.
Priority: typed scrutinee and arm identity, declaration-owned variant/arity,
explicit default coverage, then a stable diagnostic. Owners: admitted enum
declarations, statement type rows and parser-owned arm facts; the common body
verdict is the last consumer. Do not rescan source text or defer failure to a
backend, treat a duplicate arm as a new variant, merge nested match arms, or
turn a native warning into a new rejection. Falsifier: word_deletion's
04_match_enum/neg_orig.pgy; controls cover complete/default/nested matches and
payload arity. Invalid sources are admission-only. Primary integrates through
source_admission_parity.sh; no independent implementation lane is open.

Reached build blocker: the valid enum owner uses a String variant name in one
scope and an Int variant index in another. Native C picks the first spelling's
type for a later loop phi. The independent lexical_local_type_identity_valid
fixture preserves that falsifier. Objective: C phi types come from exact SSA
incoming definition/parameter type facts, never a base-name lookup. Compute
one routine-local projection, check missing/conflicting inputs before emission,
and forbid the old spelling path for phi results. Keep non-phi consumers
outside this bounded change. Native/public execution and a malformed phi
type/edge gate are the integration boundary before returning to enum admission.

The phi gate exposed its upstream cause: assignment DEF ABI typing also joined
locals by spelling. The lexical scope resolver now captures the exact binding's
checked type on the assignment; MIR consumes that receipt without a name lookup.
The LLVM assignment alloca is the last reached consumer and must require that
ABI fact as well. The same valid source must print alpha,0,1 in all four paths;
a missing assignment ABI or crossed phi edge must refuse before target emission.
The enum default-only control also reaches a parser ambiguity: reserved default
must stay a match arm, not be consumed as a nominal field initializer. Keep this
valid control, not a rewritten spelling that hides the public parser gap.

Production admission now refuses the five enum negatives; the executable gate
also reaches valid default/duplicate/nested and non-enum partial matches. The
MIR match CFG owner must preserve their real continuations: no invented live
merge when all default-covered arms terminate, and no required preceding case
for a sole default. Direct-MIR expression/terminal admission consumes owned
pattern and CFG identities; it must not guess total coverage from arm count,
reject a redundant-but-total arm chain, or waive a reachable missing return.
Integration: named_enum_match_admission.sh plus source_admission_parity.sh;
crossed match/CFG controls remain admission-only.

Next reached seam: retry metadata must not authorize single-execution output.
The parser already owns IntentRetry rows; the kind inventory and intent
signature identity locate them at common semantic admission. Positive counts
remain unavailable, with the native INTENT_STEP_INVALID receipt identity,
before public MIR/C/LLVM or an inspection manifest can imply executable support.
Unused retry-bearing declarations also reject; the same intent without the
modifier stays valid. Native parser requires a strictly positive count, so
retry(0) is a rejection control, not an accepted no-op. Do not rescan headers
from an emitter, invent a retry runtime, or claim syntax parsing is execution.
The registry keeps all 146 identities but marks the three unimplemented
resilience surfaces with support mask 0. Its generator permits only contextual,
unpromoted unavailable rows; native lookup and public metadata project this
same owner. Regenerate projections/inventory through the generator and run the
registry gate. Reserved timeout/backoff must emit a parser diagnostic, not a
silent Exit. This is support-claim correction, not new language surface.

## Parked and verification boundaries

Reached declaration-admission seam: action within/authorized-by. Objective:
native and public admission agree that an authorized binding must resolve to
the action's self/subject parameter and to an explicit authority-bearing subject
slot in the declared zone. Priority: exact callable/binding/zone/slot identity,
native-compatible declaration rules, missing-fact refusal, stable receipt.
Owners: admitted signature action contracts, nominal field rows and zone
authority rows. The common body admission verdict is the last consumer; no
backend header rescan or type-only authority inference. Source falsifier:
word_deletion/06_action_func/orig_within.pgy. Controls retain uncalled valid
actions, explicit authority, ordinary within without authorized-by and distinct
same-type slots; negatives cover missing/wrong zone, binding and authority.
The sibling func-within parse refusal must report its action-only diagnostic
instead of silently exiting. Integrate the focused action admission gate with
source_admission_parity.sh. Invalid inputs are not executed. No parallel lane.

The action declaration gate is now green; its valid uncalled-action execution
control exposed a pre-existing public LLVM receiver-signature limitation.
Keep that integration row RED (do not omit the method or waive the test).
The bounded common-admission rung continues with the same action contract's
required abilities: subject ownership, declared ability identity, ordered
generic arguments/defaults and a matching role impl. Reuse admitted role facts;
do not infer an impl from method spelling. Falsifier: 21_ability_role/neg_orig.pgy;
retain orig.pgy and explicit/default generic requirements as positive controls.
The receiver/typed-parameter connection in DirectMirRoutineInstanceInventory is
a mandatory remaining execution obligation, not a claimed closure.

Next common-admission seam: Future/RemoteFuture completion lifecycle. Objective:
every normal exit retires owned handles by await or explicit own-parameter
transfer; Cancel never discharges the join. Priority: lexical binding identity,
branch/loop/scope flow, explicit ownership, native diagnostic receipt. Owners:
typed local/signature/scope and expression facts, with the native lifecycle
checker as the behavioral comparator. Last consumer: common body verdict before
source C/MIR/LLVM. Do not count await spellings, rescan backend source, invent an
implicit join/cancel, or promote borrowed/aliased handles into owned ones.
Falsifier: 13_spawn_await/neg_orig.pgy. The focused future_lifecycle_admission.py
baseline covers returns, cancellation, both/one branch, loop exits, aliases,
mutable bindings, immediate await and own parameters; no input executes.
Cancel-then-await is a required positive; its undefined-function public verdict
is an additional reached typing gap, not a reason to weaken the control.

The Future controls reached two native comparator defects. A nested resource
declaration must enter ResourceFlowUniverse before its lexical scope is popped;
function-end/snapshot discovery is too late. Conditional `&&`/`||`/`??` operands
reuse the existing resource snapshot/merge owner, never unconditional RHS
consumption. Keep same-name nested Future and skipped/conditional RHS controls.
Public match flow consumes the completed enum coverage owner's compact node-ID
receipt instead of recounting arms. Mutable-let mode remains parser-owned;
standalone spawn/await expression statements must reach the existing graph-kind
binder and common semantic verdict. Cancel payloads require admitted primitive
or enum identity and cannot invent cleanup for ownership-bearing values.

The next executable falsifier is the retained action_authority_valid public
LLVM control. Objective: admit and emit its typed mutable-identity receiver
through the existing direct-MIR routine inventory and identity-cell facts.
Priority: owner declaration/receiver identity, parameter ABI, field-store
carriage and all-routine verification, then patch size. Last consumer: shared
scalar GraphPlan materialization. No omission of uncalled methods, spelling
fallback, source rewrite, new reachability engine or malformed-MIR waiver.
Gate: the existing source-admission integration row plus focused crossed
receiver/owner/parameter controls, before broader bootstrap. Primary only.

The bounded shared receiver gate and production-driver integration now pass.
The routine-index declaration identity authorizes implicit self,
and one IdentityCellStore receipt (operation 45, GraphPlan v81) reaches C/LLVM.
Native assign and public SSA-def are explicit wire shapes, not a fallback
selection. Calls resolve source ID before validating receiver and display name.
Keep the uncalled-action control, same-named foreign-owner controls, writable
field types and invalid ordinary-null-parameter controls. Do not promote this
bounded scalar-cell rung into generic/all-aggregate receiver closure.

Next reached seam: the parser's synthetic loop condition. Objective: `loop`
and `while true` carry the same owned Bool-literal fact into statement typing
and MIR, without a backend spelling repair. Priority: correct typed literal,
body/control-flow preservation, negative typed-condition gate, production
execution. Owner: `parser/stmt_loop_owner.pgy` using the existing scalar
expression owner. Last consumer: common statement verdict and MIR loop lowering.
Falsifier: `word_deletion/cases/27_loop_while/orig.pgy`; preserve break/continue,
nested/conditional bodies, and non-Bool/unknown condition refusals. Primary only.

The parser fix reached two production LLVM falsifiers: a loop ending in a
Void return and a false-condition loop with no locals. Replace the String-only
single-routine route with a scalar control-flow claim; the existing shared
GraphPlan still admits each expression, edge and return. No fixed block-count
certificate or failed-admission fallback may decide these scalar programs.
The routine signature owns Void-return meaning; native empty-source/Void-ABI
and public AST_RETURN_VOID/absent-ABI are distinct checked wire shapes.
Gate: `loop_statement_execution.py`, shared projection from both MIR producers,
and refusal-only crossed condition/return controls; retain String branch parity.

The loop control-flow rung now passes its production/shared gate. Next owner
card: native-positive `word_deletion/cases/26_unsafe_block/orig.pgy`.
Objective: preserve the existing unsafe statement's effect and lexical scope
in public admission and execution. Priority: typed block identity, owned
EFFECT_UNSAFE fact, declared/transitive effect checks and parallel refusal,
then body emission. Parser/HIR own the node; existing callable effect equations
own propagation; common body verdict is the last admission consumer. No
effect-free block alias, native preflight or backend-only check is allowed.
Gate: original valid body and lexical-flow controls on native/public C/LLVM,
with missing effect, nested/parallel context and invalid body as admission-only
falsifiers. This is the only active implementation lane.

Reached execution falsifier: nested unsafe scopes reuse the source name `n`.
Public MIR already carries distinct declaration LocalRefs and correct SSA uses,
but MIR-to-AST C reconstruction flattens those bindings. Move the admitted
identity-required single-routine scalar C family to the same GraphPlan as LLVM; do not
repair user source names or retry reconstruction after failed GraphPlan admission.
Empty Void bodies require an exact empty CFG, not a fabricated operation or
return value. Native lexical SSA identity remains a separate comparator defect
within this same falsifier; its current wrong output is not an accepted oracle.
The LocalRef wire owner decides whether identity carriage is required before
this C route is claimed. A primitive-local-only route also captured Log(1.5),
which is outside scalar GraphPlan; retain this valid C regression. Unclaimed
expression families keep their existing C owner, not an admission-error retry.

Native continuation objective: carry the semantic resolver's declaration
identity through HIR definitions/phi seeds and MIR SSA uses. The reached
`Symbol.decl_syntax_id` -> identifier -> HIRLocalBinding -> MIR binding/SSA use
carriage is now implemented. The second same-spelled let is retained; native
C/LLVM consume exact instruction versions instead of an ambient name alias.
Last consumers remain native MIR DEF/use construction and backend projection.
No source renaming, synthetic outer-storage restoration or `.1` storage repair.
The fixed source produces 2,3,1; retain formal, initializer, branch and
different-type shadow controls. Native source-local wire inventory and legacy
RIR spelling-only resource operands still need separate reached evidence.

The production driver now exposes a loop-summary omission: unsafe's body
reaches CFG lowering, but `mir/loop_reachability_fact_owner.pgy` omitted its
continue/return facts. That same owner must include the body's flow before
header SSA planning, preserving the disagreement refusal rather than waiving it.
The unsafe gate's loop-flow and unreachable-tail controls are the falsifiers.
Current build/gate status belongs only to the handoff.

The remaining loop disagreement reaches native inout copy-out: the last
consumer is a function exit, not merely its return expression. Carry the formal
declaration's current SSA version as a MIR use and let DCE follow that edge;
delete its name-based parameter exception. LLVM copy-out queries the same
routine-owned key order, not all exit names sharing a spelling. The implicit
Void exit must be authorized by the checked semantic return type, not a guessed
backend default. Gate: inout branch/shadow, existing loop-flow and MIR's exact
copy-out/absent-identity checks. No separate implementation lane.

Active public continuation: primitive value-result parameters. Objective: make
public C/LLVM use the same admitted formal identity for entry, assignment,
nested calls and normal-return copyout. Priority: signature carriage/type,
exact parameter LocalRef, one selected storage location, then projection.
Owners: the callable parameter role and existing GraphPlan local/parameter
inventory; last consumers: C/LLVM signatures, expression reads and return
materialization. No name-based shadow join, unchanged entry-value copyout,
signature-only acceptance or failed-admission MIR-to-AST retry. Retain the
inout branch/shadow/call-shadow controls in `unsafe_block_execution.py`, add
different-type/unchanged/return-call controls, and refuse crossed identity or
carriage before execution. Native C parameter storage and world-field binding
repairs support this same reached rung; broad world LLVM ABI is still open.

The reached native-MIR comparator now needs the same carried identity. Its
source-local inventory must retain lexical declarations under unsafe (including
same-spelled declarations), and expression formal binding must join the
semantic declaration ID rather than its name. The native MIR dump projects
those owned identities into the existing conditional LocalRef wire; it must
not invent a second source declaration or rewrite SSA display names. The
`scalar_value_result_identity.py` native shared-admission row is the falsifier;
public crossed-formal-owner refusal stays required. Constant-folded native
unreachable blocks separately remain a recorded shared-CFG input obligation.

Preserving duplicate local spellings exposes the assignment target-mode join
during the production probe build. Keep that mode with the resolved root's
declaration ID and host-field kind: parameter, source-local and hosted-field
inventories verify the same ID. The population and validation consumers may
not count unrelated declarations by spelling. The scalar shadow/reassignment
MIR unit and the full import-composed probe build are the falsifiers; do not
restore name deduplication or waive the mode check to make either green.

Next active executable rung after scalar integration: the three native-positive
Intent sources `12_guard_family/{pre,invariant}.pgy` and
`28_intent_header_policies/orig.pgy`. Objective: public parsing/admission must
preserve their existing clause meaning, not silently exit. Priority: native
clause semantics and typed identity, common admission, downstream plan/runtime
carriage, then projection. Reached owners: `parser/decl_intent_owner.pgy` and
`parser/intent_policy_clause_owner.pgy`; locate the existing typed artifact,
DIR/Intent execution-plan owners and last backend consumers before edits.
Forbidden: accepting then erasing pre/invariant/rollback, aliasing them to a
different check phase, silent Exit(1), or native-parser fallback. The three
valid source admissions, crossed/missing clause facts and fixed valid execution
controls falsify the rung. A diagnostic-only repair is not feature completion.

The explicit-full positive reaches an eager binary ordering defect: the Intent
call and subsequent participant observation agree when split into statements,
but nested C operands observe the field before the call. The source contract is
left-to-right, not whichever native backend happens to emit. Use the existing
ordered expression children at native/public C materialization; no SequencePlan,
effect-name scan, source rewrite or eager evaluation of short-circuit operands.
Hygienic operand storage must not capture source bindings. The unchanged Intent
expression, its separate-statement control, eager scalar/String operands and
short-circuit controls are the gate. This is a prerequisite reached by the same
Intent rung; public LLVM Intent shape support remains an independent obligation
within it. The 2026-09-08 boundary-closure proposal confirms this priority, not
permission to open Future/runtime/erasure implementation lanes in parallel.

Reached ordering prerequisite: dynamic Int division. The runtime already owns
`pgy_checked_div_i32_export`; normalized checked-Int admission must select its
stable ABI row and preserve i32 truncation/check/result sign extension at the
last C/LLVM consumers. Append the manifest row without renumbering existing
rows. Delete raw Int sdiv and literal-only readiness together; no checked-Long
substitution, unchecked division or source-value guess. The valid effectful
divisor control, negative crossed ABI/type facts, runtime manifest projection
and old-path residue check falsify this ownership claim.

The next reached Intent consumer still compares declaration wire `kind` with
`subject`/`zone`. `mir/declaration_wire_kind_owner.pgy` owns their canonical
class container, while `nominal_kind` preserves language identity. Migrate the
existing legacy-Intent route to that owner; do not accept both old and new
spellings or add another fixture route. Its existing production fixture and
crossed nominal/container checks falsify the claim. Main expression-shape and
phase-carriage limitations remain real execution obligations after selection.

The pre/invariant rung keeps source identity through parser/HIR and DIR, then
uses `mir/intent_phase_contract_owner.pgy` for producer/consumer vocabulary.
One invariant graph has two runtime checks, not two source binders: its MIR
pre/post pair must have equal admitted graphs and both be present. C checks
run after placement/receiver binding, before on, and again after on for the
invariant. Common admission rejects non-Bool/current-outcome reads. Typed-enum
execution still lacks a predicate-failure transition; that combination must
refuse rather than erase its predicates. Source observations and crossed-phase
MIR refusal are the integration gate; parser or diagnostic success alone does
not close the rung.
The native invariant resolver must also run before outcome-scope introduction:
the same invariant executes before on. The source-only current-outcome falsifier
was accepted natively while the Pergyra owner correctly refused it. Move that
existing check, preserve its exact binding identity, and never execute this
invalid source to compare backends. The independent native semantic unit and
`intent_predicates.py` own refusal evidence.

Reached prerequisite within the same Main/Intent rung: the legacy LLVM graph
receipt wrongly obtains Intent participant names from Main's local names.
Objective: separate these lexical scopes and join the call by ordered admitted
Intent bindings, exact target identity and typed Main operands. Priority:
binding identity, no spelling fallback, execution, then patch size. The existing
`MirIntentBindingProjection` owns formal order; the graph receipt consumes that
projection once, and the legacy plan/emitter are the last consumers. Do not add
a second binding census or route on fixture names. Alpha-renamed/cross-named and
reordered-formal calls must retain their independent true/false observations;
crossed call target/order facts must refuse without an artifact. The existing
legacy Intent LLVM gate owns integration. Its bounded Main expression envelope
remains open after this prerequisite; this is not general Main closure.

Next reached prerequisite: ordinary GraphPlan lacks Zone subject-slot storage.
Objective: carry nominal slot identity and contained fresh Subject storage through
the existing identity-cell facts, expression arena and C/LLVM consumers. Preserve
noncopy identity and generation state; no passive-record reinterpretation or
source rewriting. Declaration field identity owns slot kind/type; the common
cell lifetime gate owns containment and refusal of escaping/aliased backing.
Only synchronous frame-owned cells are in this substitution; runtime topology,
Zone call/return ABI, sync/Intent protocol calls and clone/transfer need their
own reached facts. Do not admit Zone value parameters as an internal shortcut:
the source contract requires an admitted resource boundary (read-only `ref`
already has source admission, but still needs its exact target carriage).
Verify declaration-order independence, two distinct subject slots and ordinary
routine/field composition on all source legs, with crossed slot/backing facts
refused without execution. Keep the existing legacy Intent gate unchanged as a
regression boundary; general Main/Intent integration remains the active rung.

The next falsifier is `tests/self_hosted/parity/fixture/zone_readonly_call.pgy`:
source admission carries `readonly-ref` / `indirect` for `CounterZone`, while
the callable route rejects that type/carriage. Native C/LLVM and public C
observe exactly 17. Join the existing parameter role/target carriage and the
cell lifetime boundary before allowing the public LLVM call; do not convert
Zone ref into a value parameter or expose an unqualified internal pointer.
The original full-policy Intent Main remains the end-to-end falsifier after
this prerequisite. Zone topology/sync and Intent protocol need their existing
owned plans, not the storage receipt alone.

Readonly Zone call closure uses the existing callable parameter policy for
nominal-kind/carriage, the signature receipt for exact indirect/resource/ABI
shape, and the cell lifetime owner for nonescape. The C/LLVM call projections
consume the cell address, not the address of a local pointer slot. Reborrows
must retain readonly carriage; cell returns and slot mutation remain refused.
Both MIR producers must agree: the reached native signature capture marked
Zone readonly refs direct because its nominal branch only included class.
Correct that existing MIR fact owner, not the JSON writer or consumer, and
refuse the old direct-ref wire. The receiver execution gate covers single and
mixed/reborrowed arguments, crossed ABI/carriage and direct lifetime checks.
The next reached native fact was also wrong: RIR initialized a readonly Zone
formal as owned and MIR invented its detach/invalidation edge. Capture
`BorrowedRead` from the source mode before RIR state initialization; the MIR
cleanup owner excludes only that borrowed formal from implicit owned cleanup.
Explicit invalidation operations and other owned/Intent resources keep their
existing cleanup obligations. RIR initial/final state, MIR edge absence and
legacy Intent regression falsify this correction; do not suppress cleanup in
the GraphPlan consumer.

General Main/Intent call integration first needs the lost formal binder fact.
Intent routine `params` is empty and legacy semantic carriers retained only
alias/type/kind; using Main locals or inventing IDs from row order cannot make
an owned callable signature. Preserve the existing DIR participant node ID and
native MIR captured source ID on IntentBinding and its participant/value mirror.
The MIR instruction owner carries the ID; the binding projection checks
positive distinct binder identities and mirror agreement, then the existing
Intent plans consume them. No new syntax/IR layer, caller-name fallback or
whole-program reconstruction. Missing, duplicate and crossed binder IDs must
refuse at common carrier admission; native/public source execution and current
Intent regressions remain the integration gate. This prerequisite is not the
general Main executable replacement, which still remains required afterwards.
The IDs now reach the common binding projection and legacy graph seal; retain
their identity when the ordinary callable signature joins this Intent plan.
Both producer spines are verified at the binding owner. CFG roles now replace
fixed cleanup offsets, and both typed compensation producers reach the existing
MIR-to-C projection. General Main/direct-GraphPlan integration remains open;
neither progress item licenses another fixture-shaped emitter or a fallback.
Exact candidates and failures are recorded only in the current snapshot.

The reached signature seam is the common direct-MIR callable inventory.
Intent kind and ordered participant/value formals now reach it, and the legacy
graph's formal identity consumer uses that signature.
The binding owner remains authoritative; do not invent source IDs or insert
Intent into the semantic function/action owner. Participant carriage is the
existing Intent protocol's indirect binding, not ordinary `readonly-ref` or
value-result. Scalar value formals retain their direct value ABI; unsupported
aggregate value ABI cannot be guessed. The common signature is not permission
to run an Intent body through the ordinary function GraphPlan without its
purpose/placement/cleanup plan. Last consumers: routine instance inventory and
legacy Intent graph. Falsifiers: both producer signatures, crossed binding
identity/mode/ABI, ordinary-function refusal of the unlowered purpose body,
and retained native/public C/LLVM execution. This moves a required callable
fact, not the still-open general Main execution completion claim.

The next reached consumer is the common callable parameter-role policy. It
must classify an admitted Intent participant as the existing indirect identity
cell binding, not reject or rename it as an ordinary Zone ref/value parameter.
Keep the separate callable-signature execution gate closed until the purpose's
placement/cleanup plan exists. Both producer binder probes and the observed
completion Main falsify this seam; parameter readiness alone is not execution
support. No new routine role or backend path follows from this classification.

The retired legacy executable mirror/text lookup stays absent. The reached
owner move now separates step admission from AST-tree projection: the existing
participant/action/outcome and placement joins issue one routine step plan;
the tree consumer projects it, and common GraphPlan must consume that same
plan when its execution lowering is connected. Priority remains stable phase
occurrence and binder identity, one decision owner, missing-fact refusal, then
patch size. Do not duplicate those joins in a second backend planner or remove
the callable execution gate on the strength of a signature/step plan alone.
Last current consumer: MIR Intent tree projection; last target consumer:
ordinary GraphPlan placement/phase/cleanup lowering. Repeated-on completion,
removed-mirror refusal and typed compensation are the integration gate; the
tree must no longer perform participant/action/placement admission. No Main
envelope, new top-level IR/fact family or runtime policy change is authorized
by this extraction.

The common execution consumer must also receive the admitted routine mode,
priority, binding/phase inventory and cleanup contract without invoking AST
tree reconstruction. Move the existing routine admission into the same Intent
family and let tree projection consume its result. GraphPlan lowering must use
that result for real placement, phase branches and cleanup; it must not rebuild
those joins, interpret source text, or accept a signature as execution evidence.
The exact next execution falsifier remains `header_success_observed.pgy` on
both producer MIR inputs through common direct C/LLVM. Existing completion and
typed compensation gates guard the consumer migration; their success alone is
not the GraphPlan execution result.

The reached placement gap is the selected Zone slot's declaration identity.
Keep its alias/unique-compatible selection in one Intent policy shared with
the existing source-C adapter; join that result to the MIR declaration index
once and carry the exact slot SyntaxNodeId with the step plan. General
GraphPlan must project that identity through its cell layout instead of
scanning fields or aliasing the Zone slot to the participant. Named selection
with multiple same-type slots, unique differently named selection, a foreign
Zone's same-spelled field and a changed slot kind are the falsifiers.

The reached target operation joins that exact slot identity and its Zone and
Subject declaration rows to `logical_record.identity_cells`. Its C/LLVM
materialization copies Subject state into the existing slot storage and back;
it must never replace the slot pointer with the participant pointer. Preserve
Int/Bool/String state and check two same-type slots as well as a differently
named unique slot. Generation/synchronization, phase CFG and compensation
remain separate required operations of the same active Intent rung. A passing
transfer kernel cannot open the callable-signature execution guard by itself.

The nested control also reaches a producer disagreement: native emits phase
`intent`, while self MIR used `on` and its phase owner rejected native input.
Preserve the native protocol's distinct action/nested phases through the shared
phase owner and DIR target-kind fact, and cross-check graph target kind/ID.
Do not accept two labels as a compatibility fallback. Both producers' existing
nested priority observation and swapped-phase refusal falsify this repair.

The existing signature cap is retained while duplicated header authority moves:
the routine-header inventory owns exact header identity/array-envelope checks
for common and legacy generic signatures; parameter admission owns the Intent
participant/value representation rule. No new owner file or relaxed limit.
Missing/duplicate headers and resealed parameter mode/ABI mutations falsify
that move; the current Intent and generic execution gates are the integration
boundary. This remains part of the callable seam, not a new work lane.

The reached execution boundary requires producer-owned Intent block roles.
Native legacy MIR puts the executable statements in a separate body block;
self legacy MIR puts them in the entry block. Preserve the existing
entry/body and cleanup/rollback/invalidation roles in MIR block facts, then
consume those roles and normal edges instead of fixed block offsets. Fact owners: native
MIR routine/CFG and self MIR Intent CFG construction; last consumers: common
Intent carrier/cleanup admission and the existing execution plans. Missing,
duplicate or crossed roles, unknown body rows and disconnected executable
mirrors must refuse. No block-count alternative, ignored body or source/AST
fallback is allowed. The original general Main falsifier remains required;
its native terminal return also differs from the legacy fixed Main envelope.

Typed native MIR also retains a detached HIR mirror block; it is not executable
body. The typed construction owner must mark that role explicitly. The admitted
execution plan remains the only execution authority. Retained mirror graphs
must exactly cover the plan multiset before erasure; a producer with no redundant
mirror inventory must not reconstruct it. Missing plan facts still refuse.

The reached typed graph handoff must select the expression's call root by the
admitted action declaration ID. Source and canonical member spellings are
cross-checks derived from that same callable owner, never alternate target
resolvers. Keep the ID through projection; reject absent/foreign IDs and crossed
spellings. Both producers and the identity-prefix owner are the falsifiers.

The general Main rung also reaches legacy completion predicates. A declared
success expression must retain its source node and graph through the existing
DIR result contract and MIR phase owner, then reach the terminal consumer under
the enclosing purpose identity, never a step alias. Preserve explicit absence
separately from a lost expression. Ordinary GraphPlan owns Main/cell layout;
the old Intent emitter's inline Zone layout cannot be pasted into that module.
The first falsifiers are false and effectful success predicates after placement,
then missing/crossed header facts at both producer boundaries. Do not infer a
new failure-predicate policy, erase a clause, or call this prerequisite general
Main/LLVM closure.

The reached MIR round-trip also normalizes method target spelling at the
existing identity-epoch owner. The admitted routine ID chooses the method;
source-local/canonical names only cross-check that same declaration. Build the
canonical-ID name view once, not by scanning AST roots or selecting a target by
name. Crossed IDs/names must refuse. Keep phase construction in explicitly typed
local arrays: constructor-context empty-array inference and member-array inout
are not available on this compiler rung. The existing component gate prohibits
ArrayPush/ArraySet on projection fields; do not remove it to shrink the owner.

- The user reopened the reported remaining gaps on 2026-09-07. On 2026-09-09
  the user superseded the publication hold and requested commit/push now for CI.
  Queue, not parallel work: HashMap execution first,
  then effect/collection execution, participant authority, effect/class
  distinction, generic ability constraints, live-zone escape and typed Intent
  v3 publication. Name each reached owner/falsifier before its implementation.
- Nested callable ABI, public `Now` emission and field/record backend positives
  also remain reported execution obligations; verify them at integration.
- The Set-index diagnostic pin was corrected to the owned
  `array_index_type_mismatch`; focused local evidence is not remote CI closure.
- Module Build, constructor performance, broad ResourceCarriage redesign,
  automatic resume revalidation and research proposals remain parked.

Budgets: static 60 s, focused 300 s, integration 1,800 s. Invalid programs are
checked only for admission, never executed. Do not relax gates or increase
allowances. The current snapshot is authorized for commit/push before all gaps
are fixed, so CI can evaluate it; full verification/bootstrap remains owed.
Preserve inherited dirty work,
keep build candidates isolated, and review the complete diff before staging.
Refresh the single snapshot, not another log.
