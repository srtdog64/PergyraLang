# Zone Value Carriage Semantic Admission — 2026-08-31

Status: `LOCAL GREEN — PUBLICATION AND EXACT CI PENDING`

Exact base: `5a2a95c0dfdd956bbecdd7398874cb7c312d6386`, equal to
`origin/main` when this rung opened.

This directive coordinates one reached production ownership seam. It does not
add a fixed source-level lifetime, define move syntax, or claim that broader
zone parameter, return, or embedded carriage is complete.

## Objective card

- Objective: reject named local `zone` copies and every local `zone`
  reassignment during admitted semantic body analysis until an explicit
  transfer owner exists, then delete the C backend's thread-safe-only
  `#error` decisions.
- Priority order: preserve zone identity; make the target-neutral semantic
  boundary own carriage admission; fail closed on missing or malformed facts;
  delete backend policy reconstruction; retain fresh direct construction and
  its generated hidden-lock lifecycle; then preserve diagnostic stability.
- Production entrypoint: current-source
  `pgy-self-driver --emit-c-artifact-verified SOURCE OUTPUT`.
- Fact owners: `SemanticAstNominalConstructorFacts` owns nominal identity and
  kind; `SemanticAstLocalBindingFacts` plus
  `SemanticAstInitializerTypeFacts` own local declaration and admitted type;
  the carried expression graph owns fresh direct-constructor identity;
  `SemanticAstAssignmentFacts` plus `SemanticAstAssignmentTypeFacts` own the
  assignment target and admitted target type. The carriage verdict is a
  subordinate decision within the existing
  `SFSemanticAstArtifactAdmission` family, not a new top-level authority.
- Last legitimate consumer: `SemanticAstBodyTypeBundleFromAdmittedAnalysis*`
  joins those admitted facts once and returns the first body diagnostic.
  `EmitStmtList` may consume an admitted fresh-zone lifecycle fact but may not
  decide whether copying or reassignment is legal.
- Direct bypass to delete: `EmitStmtList` accepting both programs and emitting
  `Pergyra zone local copy requires an admitted transfer plan` or
  `Pergyra zone reassignment requires an admitted transfer plan` only as
  conditional C preprocessor errors.
- Forbidden fallback: thread-safe-mode-only rejection; backend nominal-kind
  lookup as language policy; source-text constructor classification; missing
  graph or type facts treated as non-zone; single-threaded copying as an
  implicit compatibility path; automatic lock copying/reinitialization; or a
  user-visible fixed world/zone lifetime.
- Verification gate:
  `tests/self_hosted/parity/domain_runtime_zone_sync_execution_owner.sh` must
  keep fresh-zone lifecycle execution exact, make both negative sources fail
  before any C artifact is published with stable semantic diagnostic codes,
  and reject reintroduction of either backend `#error` string. The current
  baseline falsifier exits zero and publishes C containing those two errors.

## Scope and budget

- Allowed edits: one responsibility-named semantic verdict owner, the existing
  body-bundle join, deletion of the two statement-emission policy branches,
  the focused zone gate, component/SoT ratchets, the ACTIVE registry row, and
  current collaboration/handoff documentation.
- Independent edit scopes: none. The primary task is the sole implementation,
  integration, and publication owner for this executable rung.
- Forbidden overlap: no move/borrow syntax, by-value parameter or return
  design, embedded-zone transfer, native backend redesign, general lifetime
  calculus, query/cache work, or unrelated SoT row.
- Allowed commands and validation budget: static owner gates first; focused
  semantic/codegen parity within five minutes; reuse the just-built
  current-source installed driver for production negatives; one bounded
  current-source driver rebuild only if the semantic edit requires it; broader
  matrices only at the exact pushed CI boundary.
- Integration owner and gate: the primary task owns the final diff and exact
  candidate. `domain_runtime_zone_sync_execution_owner.sh` is the focused
  falsifier; the production installed-driver negative probes are the
  integration boundary.

## Baseline evidence

- Current installed driver SHA-256 is
  `C873E3E1625C8F38E39D7EC006D980E93F606446605E22B38020ECF3B8AF2102`,
  rebuilt from the exact base above.
- Both copy and reassignment fixtures exit zero through
  `--emit-c-artifact-verified`. The resulting C artifacts contain the
  backend-only `#error` strings, proving that semantic admission currently
  accepts both invalid carriage forms.
- This rung keeps 88 authorities / 183 carriers /
  `CLOSED=55 BRIDGE=32 ACTIVE=1` and the 83% integrated forecast unchanged
  unless executable evidence later proves a separately registered closure.

## Output classification

The output is an implementation candidate for one reached semantic-admission
seam. Documents and tests are supporting evidence; only removal of the
production backend bypass plus observed semantic rejection counts as
substitution progress.

## Current candidate evidence

- Current-source focused execution is green. The gate builds the current
  `src/self_hosted/codegen/main.pgy` instead of silently reusing a stale
  `.tmp/codegen.exe`, then reports
  `copy/reassignment semantic admission and backend error ratchet: PASS` and
  `[domain-runtime-zone-sync] PASS`.
- `SemanticAstZoneValueCarriageVerdictFromAdmittedFacts` now owns the
  target-neutral admission decision. It carries the ordered syntax-node IDs
  of admitted fresh zone locals through `SemanticAstBodyTypeBundle`, its v2
  admission receipt, and `CodegenSemanticBodyTypeFacts`. Statement emission
  consumes that row with `CodegenSemanticZoneLocalFresh`; it no longer
  reconstructs constructor or nominal-kind policy.
- The two backend-only `#error` decisions are deleted. Named local copy and
  every zone reassignment now fail before C publication with
  `zone_value_copy_requires_transfer` or
  `zone_value_reassignment_requires_transfer`. Direct construction and the
  existing explicit one-argument `Clone` ownership boundary remain admitted.
- The rebuilt production DRV-2 SHA-256 is
  `18B60B93C8B5B324F3D98337F027921E57D0DF2392F75C45E6AC37E02ABC9D33`.
  Both negative fixtures exit 1 with no output artifact. The positive
  lifecycle fixture executes exact `7 / 11 / 13 / 17 / 19 / 23` output in
  single-threaded and thread-safe modes with seven lock initializations,
  thirteen generated destroy sites, and explicit `cloned` init/destroy.
- Component, compiler-world, hard-contract, SoT-edge, build-source inventory,
  documentation-quality, and agent-boundary gates are local green. SoT stays
  at 88 authorities / 183 carriers / `CLOSED=55 BRIDGE=32 ACTIVE=1`; by-value
  and embedded zone transfer remain open.
- `tests/test_inc_size_smoke.sh` still reports the pre-existing, unmodified
  `src/parser/ast_expr_control_accessors.c` at 725/699. This rung does not edit
  that owner or claim the broader size gate green.
