# Execution, projection, resource SoT closure dependency map

Audit base: `464a907a010b745c3ec1bdaecf783bbf9e31c037`

This is a static source/registry audit, not a closure declaration. No build or
test was run. `O:` records an observation from the current tree; `I:` records a
proposed dependency or executable rung. E01--E13 are local row aliases so that
predecessor cells do not duplicate registry owner IDs.

| Key / assigned owner | Current status and authority | Observed old carrier or coverage gap | Exact last live consumer | Predecessors that must close first | Smallest production executable rung | Falsifier if removed early | Negative ratchet after migration | Classification and evidence boundary |
|---|---|---|---|---|---|---|---|---|
| E01 `mir.execution_graph` | O: `BRIDGE`; `src/compiler/mir.c:52` `mir_lower` (`docs/semantics/sot_owner_spine_registry.md:68`). | O: MIR JSON expression identity is still reconstructed from `MIRInstruction.expr0/expr1` AST pointers by `src/compiler/mir_json_expression_graph.c:15` `mir_json_instruction_expression` and `:110` `mir_expression_graph_identity`; general range/foreach/nested-loop/multi-phi and cast materialization coverage remains absent in the registry row. | O: `src/compiler/mir_json_expression_graph.c:134` `mir_json_emit_instruction_expression_graph` is the last serializer that reads that AST carrier and publishes the graph consumed by installed direct-MIR routes. | I: `parser.syntax_provenance`, `hir.typed_control_flow`, `semantic.symbol_type_graph`, and `selfhost.expression_surface`; the old serializer cannot be deleted until MIR owns the equivalent stable expression/CFG identities. | I: replace AST-backed serialization for one already admitted bool/call/phi program with MIR-owned graph rows, then route both direct C and LLVM from those rows without a second graph/index. | O: `src/self_hosted/codegen/fixture/bool_logic.pgy` through `tests/self_hosted/parity/one_mir_bool_logic_projection.sh`; removing the AST graph now starves `expr*_graph` admission. | O/I: retain that gate's `bad-use-identity`, `bad-call-target`, and legacy-route rejection; add a static rejection of `inst->expr0`, `inst->expr1`, and `ASTNode` reads in the MIR JSON graph serializer after replacement. | `DEPENDENCY_BLOCKED`. O: the old AST carrier is live. I: deletion depends on stable syntax, typed CFG, and expression owners rather than another projection plan. |
| E02 `semantic.machine_layer_transition` | O: `BRIDGE`; `src/compiler/mir_machine_layer.c:8` `mir_attach_machine_layer_fact` (`registry:64`). | O: the late native enrichment seam still calls `src/compiler/mir_machine_layer.c:52` `mir_attach_machine_layer_fact_for_ast`, looks up the RIR op by AST pointer at `:62-66`, and may return success with no op at `:67-68`. Concrete board/linker/boot provision is also explicitly outside current compiler coverage. | O: `src/compiler/air_evidence_mir_facts.c:318` `air_collect_machine_layer_sites` reads and validates the final MIR transition row at `:330-365`. | I: E11 then E01, plus `target.capability_profile` and `abi.runtime_call_rows`; stable RIR-to-MIR operation identity must replace the AST lookup before the product provider is the only residual gap. | I: make the existing DeviceSlot read fixture reach MIR attachment solely by typed RIR operation identity, delete `mir_attach_machine_layer_fact_for_ast`, and preserve manifest/grant/runtime-operation validation in AIR and both backends. | O: `tests/cases/backend_compare/device_slot_machine_layer/main.pgy` through `tests/machine_layer_pipeline_smoke.sh`. | O/I: keep manifest/runtime-row rejection in `tests/machine_layer_pipeline_smoke.sh:332-379`; strengthen it to reject `mir_attach_machine_layer_fact_for_ast`, `inst->ast`, and no-op success for a required contact. | `DEPENDENCY_BLOCKED`. O: native AST recovery remains live, so the row is not yet only a `PRODUCT_BOUNDARY`; concrete hardware provision becomes product work only after that seam closes. |
| E03 `projection.direct_mir_array_int_program` | O: `BRIDGE`; `src/self_hosted/compiler/direct_mir_scalar_cfg_array_int_program_admission_owner.pgy:15` `DirectMirScalarCfgArrayIntProgramFromOwners` (`registry:75`). | O: the receipt selects a bounded foreach-pop or one local collection at `:25-70`; aliases, parameters/returns, unsealed lengths, branch mutation, general growth and arbitrary element expressions are uncovered. | O: `src/self_hosted/compiler/direct_mir_scalar_cfg_array_int_llvm_emission_owner.pgy:110` `DirectMirScalarCfgArrayIntLlvmLengthLog` is a final LLVM consumer of the sealed array receipt. | I: E01 and E06, then `abi.layout_rows` and `abi.runtime_call_rows`; arbitrary aliases/returns additionally require E13. | I: admit one nonescaping `Array<Int>` with a branch around a single push using existing ValueId/use/phi facts, and emit exact C/LLVM parity without mode retry. | O: `src/self_hosted/codegen/fixture/array_push.pgy` via `tests/self_hosted/parity/one_mir_array_int_loop_push_projection.sh`; `bad-producer-backedge` and `bad-push-edge` show the missing edge proof. | O: preserve the same gate's `bad-*` artifact non-publication and rejection of Option/block-count legacy routes (`:80-95`). | `DEPENDENCY_BLOCKED`. O: bounded modes are executable. I: branch mutation needs E01 CFG/phi coverage and E06 loop ownership before this receipt can generalize. |
| E04 `projection.direct_mir_collection_pop_effect` | O: `BRIDGE`; `src/self_hosted/compiler/direct_mir_scalar_cfg_collection_pop_program_fact_owner.pgy:52` `DirectMirScalarCfgCollectionPopProgramFromOwners` (`registry:74`). | O: the join requires both an int foreach-pop and a String collection (`:59-75`); empty pop, mixed push/pop, branch/loop mutation, aliases/returns and String destruction are not represented. | O: `src/self_hosted/compiler/direct_mir_scalar_cfg_array_int_pop_llvm_operation_owner.pgy:25` `DirectMirScalarCfgArrayIntPopLlvmOperation` consumes the sealed effect rows for LLVM; the String sibling is dispatched by the same graph-plan emission lane. | I: E03, E08, and E06, then `abi.layout_rows`/`abi.runtime_call_rows`; ownership-sensitive String pop also needs E13. | I: add one straight-line mixed push-then-pop on a nonescaping local `Array<Int>`, with ordered length-before/after rows shared by C and LLVM. | O: `src/self_hosted/codegen/fixture/array_pop.pgy` via `tests/self_hosted/parity/one_mir_array_pop_projection.sh`; `bad-pop-after-observation` falsifies effect ordering. | O: keep the gate's bad-receiver/result/order cases, artifact non-publication, and explicit legacy-route rejection (`:59-89`). | `DEPENDENCY_BLOCKED`. O: current fact is a two-source bounded join. I: a mixed effect cannot be sound until the source plans and effect order are owned upstream. |
| E05 `projection.direct_mir_collection_program_plan` | O: `BRIDGE`; `src/self_hosted/compiler/direct_mir_collection_program_plan_owner.pgy:142` `DirectMirCollectionProgramPlanFromAdmitted` (`registry:127`). | O: the plan seals the fixed carriage `producer_reallocating_array_value_main_single_cleanup` at `:34-35` and exactly producer/entry/consumer local plans; aliases, multiple collections, arbitrary reducers/effects and escaping cleanup have no stable plan rows. | O: `src/self_hosted/compiler/direct_mir_collection_program_llvm_emission_owner.pgy:8` `DirectMirCollectionProgramEmitLlvm` is the final LLVM consumer; its C peer consumes the same plan. | I: E01 and E13, plus `semantic.callable_receiver_carriage`, `abi.layout_rows`, `abi.runtime_call_rows`, and `target.capability_profile`. | I: extend the existing three-routine production route to a second independently identified `Array<Int>` argument while preserving one compile, stable call/return edges, and one cleanup owner. | O: `src/self_hosted/codegen/fixture/array_param.pgy` through `tests/self_hosted/parity/one_mir_array_param_projection.sh`; `bad-cross-routine-raw-collision` demonstrates why raw ValueId is insufficient. | O: retain `bad-repaired-param-abi`, call/return/collision rejections and the explicit ban on retrying the legacy Array-argument route (`:95-118`). | `DEPENDENCY_BLOCKED`. O: the existing plan is exclusive and executable. I: multiple collections need stable cross-routine identity, ABI, and ownership rows first. |
| E06 `projection.direct_mir_scalar_cfg_foreach_receipt` | O: `BRIDGE`; `src/self_hosted/compiler/direct_mir_scalar_cfg_foreach_admission_owner.pgy:49` `DirectMirScalarCfgForEachFactsFromOwners` (`registry:77`). | O: collection iteration is selected from `loop-init` plus supported local source types at `:15-46`; only local literal Array Int/String and a pure no-parameter returned Array Int are covered, not effectful producers or other element ABIs. | O: `src/self_hosted/compiler/direct_mir_scalar_cfg_foreach_typed_llvm_emission_owner.pgy:140` `DirectMirScalarCfgForEachTypedLlvmBlockExit` consumes the receipt's block and storage identities at loop exit. | I: E01 and E05, plus `abi.layout_rows` and `semantic.callable_receiver_carriage`; effectful producer ordering additionally needs E13. | I: make one parameterized pure producer return `Array<Int>` to one foreach consumer, reusing the collection-program call/return identity rather than discovering a producer in the backend. | O: `src/self_hosted/mir_lower/fixture/for_each_call.pgy` via `tests/self_hosted/parity/one_mir_returned_array_foreach_projection.sh`; `bad-call-target`/`bad-hoist` falsify premature widening. | O: keep that gate's producer ABI/call/hoist/local-ref mutations and its explicit rejection of legacy return-only retry (`:90-94`). | `DEPENDENCY_BLOCKED`. O: no-parameter producer carriage is closed. I: parameterized/effectful carriage depends on the cross-routine plan and ABI identities. |
| E07 `projection.direct_mir_scalar_cfg_program_extension` | O: `BRIDGE`; `src/self_hosted/compiler/direct_mir_scalar_cfg_graph_admission_owner.pgy:20` `DirectMirScalarCfgGraphPlanFromAdmitted` (`registry:128`). | O: the owner joins range, foreach, String-array, Array-Int, collection and scalar-expression plans at `:28-72`; arbitrary collection ownership, dynamic indices, owned aggregate returns, effectful short circuit, temporary Strings, recursion/multiple callables and general returns remain uncovered. | O: `src/self_hosted/compiler/direct_mir_scalar_cfg_program_llvm_emission_owner.pgy:240` `DirectMirScalarCfgEmitProgramLlvm` is the terminal LLVM consumer of the immutable graph plan. | I: E01, E03, E04, E06, and E08, plus all three ABI rows (`abi.layout_rows`, `abi.runtime_call_rows`, `semantic.callable_receiver_carriage`). | I: one effectful short-circuit program whose RHS mutates a local scalar, with the branch/phi effect order sealed once and shared by C/LLVM emission. | O: `src/self_hosted/codegen/fixture/bool_logic.pgy` through `tests/self_hosted/parity/one_mir_bool_logic_projection.sh`; `bad-short-circuit-rhs` and `bad-phi-incoming-identity` falsify deletion before graph coverage. | O: preserve that gate's artifact non-publication, scalar diagnostic, and rejection of all legacy multi-routine/collection/Option routes (`:83-113`). | `DEPENDENCY_BLOCKED`. O: the route is already a join of the other bounded projection facts. I: widening it first would turn missing child ownership into umbrella authority. |
| E08 `projection.direct_mir_string_array_push` | O: `BRIDGE`; `src/self_hosted/compiler/direct_mir_scalar_cfg_string_array_plan_admission_owner.pgy:15` `DirectMirScalarCfgStringArrayPlanFromOwners` (`registry:73`). | O: push admission stores only an ASCII literal: `src/self_hosted/compiler/direct_mir_scalar_cfg_string_array_statement_source_owner.pgy:20` `DirectMirScalarCfgStringArrayPushSourceFactFrom` requires a single string-literal graph and persists `value_literal` at `:33-43`. Dynamic value identity, aliases, return/parameter carriage, loop/branch mutation and reallocation are absent. | O: `src/self_hosted/compiler/direct_mir_scalar_cfg_string_array_llvm_mutation_emission_owner.pgy:6` `DirectMirScalarCfgStringArrayLlvmPush` consumes the literal-valued row. | I: E01 then the String-expression subset of E07, plus `abi.layout_rows` and `abi.runtime_call_rows`; reallocation/ownership widening also needs E13. | I: replace `value_literal` for one straight-line parameter-derived String push with a stable expression/ValueId row, keeping the array nonescaping and its precomputed capacity unchanged, then emit from one plan in C/LLVM. | O: `src/self_hosted/codegen/fixture/str_array_push.pgy` via `tests/self_hosted/parity/one_mir_string_array_push_projection.sh`; `bad-push-receiver`, `bad-empty-graph`, and `stale-collection` falsify unsealed widening. | O: retain that gate's structured mutations and legacy Option-route rejection (`:65-101`); after widening, add a rejection of literal-only backend recovery from `value_literal`. | `DEPENDENCY_BLOCKED`. O: the stable row currently contains only literal text. I: dynamic push must first consume an owned String expression identity, not add a second emitter read. |
| E09 `projection.verified_plan` | O: `BRIDGE`; `src/compiler/verified_projection_plan.c:184` `pgy_verified_projection_plan_intent_observability_with_air` (`registry:86`). | O: the issued row binds AIR certificate, target capability and machine manifest fingerprints (`:200-263`), but has no typed layout/cleanup/materialization artifact or self-host row. | O: `src/codegen/llvm_api.c:38` `llvm_apply_intent_observability_projection_plan` is the last LLVM consumer and rejects missing certificate/fingerprints before emission; native C has a peer in `src/codegen/transpiler_entry.c`. | I: E02 and E10, plus `air.evidence_graph`, `target.capability_profile`, `abi.layout_rows`, and `abi.runtime_call_rows`. | I: issue one typed layout-plus-cleanup row for the existing region String-concat fixture, bind it into the same verified plan, and require both C/LLVM to consume it without semantic/MIR rereads. | O: `tests/verified_projection_plan_probe.c` through `tests/verified_projection_plan_smoke.sh`; owner-fact mutation and missing target/machine fingerprints falsify premature plan use. | O: keep the gate's direct target-envelope/fingerprint read bans (`:110-126`) and AST/HIR/MIR-inventory source-inference mutation (`:129-207`). | `DEPENDENCY_BLOCKED`. O: one verified observation row exists. I: layout/cleanup materialization cannot be certified before machine, region, AIR, target, and ABI facts are bound. |
| E10 `resource.region_allocation_plan` | O: `BRIDGE`; `src/semantic/region_escape_fact.c:168` `semantic_region_escape_collect` (`registry:84`). | O: stable rows reach a verified plan, but both final backend sites still key lookup from AST: C uses `src/codegen/transpiler_context.c:358` `transpiler_region_concat` with `ast_node_stable_id(site)` at `:368-370` and silently emits heap `StringConcat` at `:379-382`; LLVM mirrors this in `src/codegen/llvm_expr_scalar_core.c:331` `llvm_emit_binary` at `:428-438`. Complete allocation ownership is absent. | O: `src/codegen/llvm_expr_scalar_core.c:331` `llvm_emit_binary` is the last live LLVM consumer of the region disposition and scope row. | I: E01, E12, and E13, then E09 and `air.evidence_graph`; stable allocation-site identity and callee retention must arrive in the plan before backend AST lookup can disappear. | I: carry the existing region String-concat AllocationSiteId through the verified plan into both emitters, delete their AST-keyed lookup, and fail closed when the required site row is absent. | O: `tests/cases/backend_compare/region_string_concat/main.pgy` and `region_user_callee/main.pgy` through `tests/region_backend_wiring_smoke.sh`. | I: extend `tests/region_backend_wiring_smoke.sh` to reject `ast_node_stable_id(` in both backend region consumers and to reject heap `StringConcat` fallback when a required plan site is missing. | `DEPENDENCY_BLOCKED`. O: the last consumers still reconstruct the key from AST and C has an explicit fallback. I: stable MIR/AIR plan carriage must precede deletion. |
| E11 `rir.resource_transition_graph` | O: `BRIDGE`; `src/compiler/rir_builder.c:295` `rir_lower` (`registry:67`). | O: `rir_lower` requires `AST_PROGRAM`, stores `program_root`, and scans top-level AST declarations to construct scopes at `src/compiler/rir_builder.c:295-407`; no complete Pergyra-owned RIR producer exists, and HIR resource-flow enrichment remains a second transition attachment seam. | O: `src/compiler/air_evidence_rir.c:43` `air_collect_rir_evidence` inventories RIR scopes/ops/facts for final AIR evidence; `src/compiler/rir_flow.c:611` `rir_enrich_with_hir_flow` is the live co-owner that attaches HIR ResourceFlow/parameter summaries. | I: E12 and E13, plus `parser.syntax_provenance`, `hir.typed_control_flow`, and `dir.domain_graph`; a typed admitted RIR scope/op/state/flow producer is the first missing predecessor fact. | I: replace native `rir_lower` for `tests/cases/semantic_loop_flow/summary_hit.pgy` on the installed internal/public RIR JSON path with one complete Pergyra RIR artifact, then feed existing validation/MIR/AIR consumers. | O: `tests/cases/semantic_loop_flow/summary_hit.pgy` through `tests/rir_resource_flow_identity_smoke.sh`; its valid identity rows would disappear if the AST builder were removed today. | O/I: keep malformed stable-identity rejection in `tests/rir_resource_flow_identity_smoke.sh`; add a post-migration static rejection of `ASTNode`, `AST_PROGRAM`, `ast_program_statement`, and native retry in the production RIR producer path. | `EVIDENCE_GAP`. O: only native AST production plus downstream Pergyra readers/projections is present. I: a complete Pergyra producer, not another consumer or plan, is the first missing evidence. |
| E12 `semantic.function_param_flow_summary` | O: `BRIDGE`; `src/semantic/function_param_flow_summary.c:505` `function_param_flow_summary_demand` (`registry:63`). | O: native demand analysis remains the sole producer and is called directly from slot analysis; the self-host side only parses/validates MIR JSON rows (`src/self_hosted/mir_lower/routine_fact_index_owner.pgy:64`, `:453`) and therefore cannot replace recursive body evaluation or native-only consumers. | O: `src/compiler/air_evidence_mir.c:170` `air_collect_function_param_flow_summaries` is the final native AIR consumer, copying routine rows with stable routine/parameter identity at `:231-260`. | I: `semantic.symbol_type_graph`, `selfhost.semantic_artifact_admission`, and E01; recursive call identity and a Pergyra semantic producer must exist before native demand/body reopening can be removed. | I: produce the `Recur` parameter mask in the production self-host semantic artifact, carry it through MIR JSON, and make AIR/self-host readers consume that row without invoking native demand. | O: `tests/cases/function_param_flow_summary/main.pgy` through `tests/self_hosted/mir_function_param_flow_summary_smoke.sh`; the valid row currently originates from `--test-native-mir-json-oracle`. | O/I: retain the count-mismatch fail-closed mutation (`:72-80`); after producer migration, make the gate reject direct `function_param_flow_summary_demand` calls from slot analyzers and any native retry. | `EVIDENCE_GAP`. O: producer evidence is native-only while Pergyra is a reader. I: owner migration cannot be scheduled as a consumer cleanup until a Pergyra summary producer exists. |
| E13 `semantic.resource_flow_universe` | O: `BRIDGE`; `src/semantic/type_checker_flow_universe.c:94` `resource_flow_universe_bind` (`registry:61`). | O: native semantic analysis is the only universe producer; HIR/MIR/RIR copy routine-local projections, while the self-host path only parses and validates rows (`src/self_hosted/mir_lower/routine_fact_index_owner.pgy:64`, `:326-374`). Typed DIR/RIR transition payload production remains missing. | O: `src/self_hosted/mir_lower/routine_lower.pgy:394` `EmitRoutineTreeWithContractAtRowWithExpressionOrder` is the installed reader that fail-closes incomplete ResourceFlow rows at `:447-452`; native RIR also copies them in `src/compiler/rir_flow.c:611` `rir_enrich_with_hir_flow`. | I: `semantic.symbol_type_graph`, `parser.syntax_provenance`, and `selfhost.semantic_artifact_admission`; E11 consumes the eventual typed transition rows rather than preceding their semantic identity owner. | I: issue the `slot` SymbolId/resource row for the recursive fixture from the production self-host semantic artifact, carry it once into MIR and RIR typed views, and remove native row recovery/copy authority. | O: `tests/cases/function_param_flow_summary/main.pgy` via `tests/self_hosted/mir_resource_flow_identity_smoke.sh`; its valid universe still comes from the native oracle and missing count is rejected. | O/I: keep the malformed count/identity rejection (`:82-101`) and `tests/dir_resource_flow_identity_smoke.sh`'s no-duplicate-DIR snapshot rule; after migration reject `resource_flow_universe_bind`/semantic-row recovery from non-owner paths. | `EVIDENCE_GAP`. O: stable projections and a Pergyra reader exist, but no Pergyra semantic universe producer does. I: the first missing fact is producer-owned SymbolId/resource membership, not another projection. |

## Dependency-order conclusion

O: every assigned registry row is still `BRIDGE`; none is observed `CLOSED` or
`ACTIVE`. The executable projection rows are bounded and fail closed, but their
remaining extensions depend on live AST/native semantic carriers or on ABI,
target, AIR, syntax, and typed-control-flow owners outside this report. The
three producer gaps at E11--E13 are not evidence that an existing plan can be
widened.

I: this scope supplies no independently justified `READY_NEXT` row. The first
integration decision must come from the union map: either close an upstream
producer/identity predecessor, or record the missing producer fact explicitly;
widening E07 or a collection emitter first would create fallback or dual
authority.
