# BRIDGE=32 successor admission audit — 2026-08-29

This is a read-only observation report, not semantic authority, progress
evidence, or an implementation queue. Current source, the SoT registry, and
executable gates override it.

## Basis and question

- Exact basis: `e54ce620d49f79c3ab817ca9afeefd8b5a0310f7` on
  `origin/main`.
- Publication evidence: exact-head CI run `33229419440` completed GREEN 30/30.
- Census: `CLOSED=55 BRIDGE=32 ACTIVE=1`.
- Question: can one bounded installed production rung delete the last direct
  bypass, supply a decisive negative gate, and honestly close one whole
  remaining row?
- Forbidden answer: registry-only reclassification, narrowing an existing row
  into a new bounded identity, generic query/cache work, or several independent
  producer migrations disguised as one atomic rung.

## Candidate verdicts

| Candidate | Verdict | Decisive reason |
| --- | --- | --- |
| `selfhost.match_case_pattern` | Not a BRIDGE candidate | It is already `CLOSED`. The remaining owner-internal text admission lacks parser-to-artifact typed pattern carriage and is not a last-consumer bypass. |
| `semantic.loop_flow_summary` | Dependency blocked | The self producer emits zero effect/state defaults, the resource universe is empty, and direct-MIR admission rejects non-empty state. Complete semantic-flow ownership crosses `semantic.resource_flow_universe`. |
| `abi.mir_array_string_layout_projection` | Multi-family prerequisite | C/LLVM terminal materializers and many mutation, parameter, value-result, process, and directory consumers reconstruct target layout. No single target-qualified projection receipt is carried through the whole emission graph. |
| `diagnostic.catalog` | Three transport prerequisites | Parser/lexer, runtime-none, and AIR each flatten failure into free text before the driver. Removing the mapper without typed `DiagnosticId` transport would lose or misclassify identity. |
| `compatibility.evolution` | Missing owner-issued receipt | Canonical rows have no carried stable row identity. Diagnostic, package-lock, source-tree manifest, and checker consumers are independent seams; valid `PGYCOMPAT003`/`004` crosswire is currently not rejected. |
| `selfhost.enum_declaration_rows` | Missing parser/artifact carriage | The lightweight checker source scan is real, but parser enum declarations and `AstTreeArtifact` still carry enum meaning as text. The semantic enum owner reparses that text, and enum methods/payload identity are not fully carried. |

## Why the count moved slowly

The recent history is executable rather than status-only:

```text
1aed1291  CLOSED=52 BRIDGE=35 ACTIVE=1
01afa785  CLOSED=53 BRIDGE=34 ACTIVE=1
d7b78575  CLOSED=54 BRIDGE=33 ACTIVE=1
42f1d723  CLOSED=55 BRIDGE=32 ACTIVE=1
```

Intent declaration rows, intent-observability ABI rows, and scalar-CFG foreach
receipts each removed a reached reconstruction path and added fail-closed
evidence. The current 32 rows are not equivalent to the stale foreach status:
the six audited candidates each have missing producer/carriage facts or more
than one independent last-consumer family.

## Admission result

No successor whole-row closure is admitted from these six candidates. The next
implementation must name one prerequisite production bypass and count only its
executable replacement delta. It must not promise a BRIDGE decrement until all
consumers in that registry row have migrated, missing facts fail closed, the
old path is deleted, and a negative ratchet prevents its return.

The most concrete future dependency chains are:

1. carry one target-qualified ArrayString ABI projection through the scalar
   program emission graph, migrate consumers by semantic execution target, and
   close only after the full consumer inventory and valid-row crosswire gate;
2. introduce complete Pergyra loop-flow plus ResourceFlow ownership before
   admitting non-empty loop state in direct-MIR;
3. carry typed diagnostic identity separately through parser/lexer,
   runtime-none, and AIR before deleting driver free-text recovery.

These are proposals for falsifier selection, not active implementation rungs.
