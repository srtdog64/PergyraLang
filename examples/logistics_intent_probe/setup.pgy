func BuildLoadingZone() -> LoadingZone
{
    let courier = Courier(
        "Ivo",
        "north-17",
        CargoState(0, 0, ""),
        0,
        0,
        "idle"
    );
    let dispatcher = Dispatcher(
        "Mara",
        DispatchLedger(0, 0, 0),
        "north",
        "idle"
    );
    return LoadingZone(
        courier,
        dispatcher,
        CourierView(courier.name, courier.prepared, courier.delivered, courier.lastPhase),
        DispatchPacket(dispatcher.name, dispatcher.lane, dispatcher.ledger.approved, dispatcher.ledger.shipped),
        "loading",
        0,
        0
    );
}

func BuildDeliveryZone() -> DeliveryZone
{
    let courier = Courier(
        "Ivo",
        "north-17",
        CargoState(0, 0, ""),
        0,
        0,
        "idle"
    );
    let dispatcher = Dispatcher(
        "Mara",
        DispatchLedger(0, 0, 0),
        "north",
        "idle"
    );
    return DeliveryZone(
        courier,
        dispatcher,
        CourierView(courier.name, courier.prepared, courier.delivered, courier.lastPhase),
        DispatchPacket(dispatcher.name, dispatcher.lane, dispatcher.ledger.approved, dispatcher.ledger.shipped),
        "delivery",
        0,
        0
    );
}

func OpenLogisticsWorld() -> LogisticsWorld
{
    let loading = BuildLoadingZone();
    let delivery = BuildDeliveryZone();

    delivery.courier = loading.courier;
    delivery.dispatcher = loading.dispatcher;

    return LogisticsWorld(
        loading,
        delivery,
        "",
        0
    );
}
