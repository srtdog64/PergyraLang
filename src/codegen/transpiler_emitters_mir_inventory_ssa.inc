/* MIR inventory / SSA emitter helpers split into sub-1000 LOC slices.
 * Keep this shim for the existing transpiler include order. */
#include "transpiler_emitters_mir_inventory_intent.inc"
#include "transpiler_emitters_mir_inventory_ssa_names.inc"
#include "transpiler_emitters_mir_inventory_ssa_emit.inc"
