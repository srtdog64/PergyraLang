#ifndef PERGYRA_AIR_VERIFICATION_HANDLE_H
#define PERGYRA_AIR_VERIFICATION_HANDLE_H

/* Public orchestration handle.  The compiler/backend bundle must not expose
 * AIRProgram or carry AIR evidence as a code-generation input. */
typedef struct AIRProgram PgyAirVerification;

#endif /* PERGYRA_AIR_VERIFICATION_HANDLE_H */
