func Main() -> Void {
    let longs: Array<Long> = [1L, 2L, 3L];
    let floats: Array<Float> = [1.5, 2.5];
    let bools: Array<Bool> = [true, false, true];
    Log(longs[1]);
    Log(floats[0]);
    Log(bools[2]);
}
