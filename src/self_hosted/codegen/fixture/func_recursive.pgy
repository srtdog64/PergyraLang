func Fib(n: Int) -> Int {
    if n < 2 {
        return n;
    }
    return Fib(n - 1) + Fib(n - 2);
}
func Main() -> Void {
    let i: Int = 0;
    while i <= 10 {
        Log(ToString(Fib(i)));
        i = i + 1;
    }
}
