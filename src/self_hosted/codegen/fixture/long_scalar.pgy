func AddLong(x: Long, y: Long) -> Long {
    return x + y;
}

func Main() -> Void {
    let a: Long = 5;
    let b: Long = 7;
    let c: Long = AddLong(a, b);
    let d: Long = 3L;
    Log(c);
    c = c + d;
    Log(ToString(c));
}
