func Main() -> Void {
    let a: Option<Int> = None;
    a = Some(7);
    let b: Option<Int> = Some(1);
    b = None();
    if IsSome(a) {
        Log(UnwrapOption(a));
        let sum: Int = UnwrapOption(a) + 3;
        Log(sum);
    }
    if !IsSome(b) {
        Log(0);
    }
}
