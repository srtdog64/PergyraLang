func Main() -> Void {
    let a: Option<String> = None;
    a = Some("ok");
    let b: Option<String> = Some("unused");
    b = None();
    if IsSome(a) {
        Log(UnwrapOption(a));
    }
    if !IsSome(b) {
        Log("none");
    }
}
