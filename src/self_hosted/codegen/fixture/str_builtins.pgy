func Main() -> Void {
    let s: String = "pergyra";
    let n: Int = StringLength(s);
    Log(ToString(n));
    let sub: String = SubstringWithLen(s, n, 0, 4);
    Log(sub);
    let tail: String = Substring(s, 4, n - 4);
    Log(Concat(sub, Concat("-", tail)));
}
