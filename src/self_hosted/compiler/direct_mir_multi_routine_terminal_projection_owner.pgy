// Terminal multi-routine dispatch after earlier nominal/role families decline.
// A program no route claims is refused with the scalar route's receipt.

import "direct_mir_three_routine_projection_owner.pgy";
import "direct_mir_scalar_program_route_admission_owner.pgy";

func CompileAdmittedDirectMirTerminalMultiRoutine(
    ref admitted: MirMachineLayerAdmittedJsonInput,
    target_projection: CompilerTargetProjectionFact,
    is_llvm: Bool,
    formatted_print: CompilerRuntimeCallAbiFact,
    line_format: String,
    ref scalar_program_admission: DirectMirScalarProgramRouteAdmission
) -> CodegenRoutePayload {
    if MirProgramRoutineIndexCount(admitted.routines) == 3 {
        return CodegenRoutePayloadEmitted(CompileAdmittedDirectMirThreeRoutine(
            admitted, target_projection, is_llvm, formatted_print,
            line_format
        ));
    }
    return CodegenRoutePayloadRefused(
        DirectMirScalarProgramRouteAdmissionRefusal(scalar_program_admission));
}
