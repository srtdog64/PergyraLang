// LLVM adapter from the sealed host-io ReadFile receipt to native runtime ABI.
import "direct_mir_scalar_cfg_graph_fact_owner.pgy";
import "direct_mir_scalar_program_runtime_abi_projection_owner.pgy";
import "../codegen/runtime_abi/host_io_runtime_owner.pgy";

func DirectMirScalarProgramLlvmReadFileBlock(
    ref plan: DirectMirScalarCfgGraphPlan,
    ref runtime: DirectMirScalarProgramRuntimeAbiProjection
) -> String {
    if plan.program.runtime_abi.read_file_id == 0 { return ""; }
    if !runtime.read_file.ok || runtime.read_file.symbol !=
            HostIORuntimeCReadFileFn() {
        Die("direct MIR scalar LLVM read-file ABI is invalid");
    }
    return Concat(
        "declare ptr @pgy_read_file(ptr)\ndefine internal ptr @",
        Concat(runtime.read_file.symbol,
        "(ptr %path) {\nentry:\n  %result = call ptr @pgy_read_file(ptr %path)\n  ret ptr %result\n}\n\n"));
}
