// Typed readiness for normalized unary String transforms.

import "direct_mir_scalar_program_expression_fact_owner.pgy";

func DirectMirScalarProgramStringTransformExpressionNodeReady(
    ref facts: DirectMirScalarProgramExpressionSet, node: Int
) -> Option<Bool> {
    let kind: Int = facts.node_kinds[node];
    if kind != DirectMirScalarProgramExprStringTrim() { return None; }
    let left: Int = facts.node_lefts[node];
    return Some(facts.node_types[node] == CompilerAbiLayoutStringTypeName() &&
        left >= 0 && left < node && facts.node_types[left] ==
            CompilerAbiLayoutStringTypeName() &&
        facts.node_rights[node] == -1 &&
        facts.node_local_rows[node] == -1 &&
        facts.node_parameter_ordinals[node] == -1 &&
        facts.node_literals[node] == "" &&
        facts.node_callable_rows[node] == -1);
}
