// Generated by scripts/render_lsp_hover_content.py.
// Source: src/lsp/lsp_hover_content.def. Do not edit by hand.
// The language keyword registry owns lowercase hover exposure; this
// projection owns presentation prose only.

func LspHoverPresentationLanguageWordCount() -> Int {
    return 25;
}

func LspHoverPresentationBuiltinCount() -> Int {
    return 7;
}

func LspHoverPresentationCount() -> Int {
    return 32;
}

func LspHoverPresentationWordAt(index: Int) -> String {
    if index == 0 { return "action"; }
    if index == 1 { return "authority"; }
    if index == 2 { return "authorized"; }
    if index == 3 { return "by"; }
    if index == 4 { return "causes"; }
    if index == 5 { return "class"; }
    if index == 6 { return "effects"; }
    if index == 7 { return "func"; }
    if index == 8 { return "import"; }
    if index == 9 { return "let"; }
    if index == 10 { return "match"; }
    if index == 11 { return "object"; }
    if index == 12 { return "parallel"; }
    if index == 13 { return "party"; }
    if index == 14 { return "requires"; }
    if index == 15 { return "roster"; }
    if index == 16 { return "struct"; }
    if index == 17 { return "subject"; }
    if index == 18 { return "tobject"; }
    if index == 19 { return "transfer"; }
    if index == 20 { return "using"; }
    if index == 21 { return "where"; }
    if index == 22 { return "who"; }
    if index == 23 { return "with"; }
    if index == 24 { return "within"; }
    if index == 25 { return "Err"; }
    if index == 26 { return "Log"; }
    if index == 27 { return "LogBanner"; }
    if index == 28 { return "LogBlock"; }
    if index == 29 { return "LogRaw"; }
    if index == 30 { return "Ok"; }
    if index == 31 { return "Unwrap"; }
    return "";
}

func LspHoverPresentationTextAt(index: Int) -> String {
    if index == 0 { return "**action** - Subject-host contract-bearing operation\n- carries an action contract pack: `within`, `requires`, `authorized by`, `causes`\n- matching intent steps may reuse this contract pack instead of repeating every clause"; }
    if index == 1 { return "**authority** - Zone mutation authority declaration\n- `authority subjectSlot` names the participant; optional `requires Ability` adds capability evidence\n- this often overlaps with action/step `requires`, so diagnostics should explain when the requirement was inherited rather than written twice"; }
    if index == 2 { return "**authorized by** - Authority contract clause\n- required when a step/action mutates authority-bearing zone state or runs secure/effectful work inside an authority boundary\n- matching intent steps may reuse this from the action contract"; }
    if index == 3 { return "**by** - Authority clause tail\n- used in `authorized by` and domain lifecycle commands to name the approving participant"; }
    if index == 4 { return "**causes** - Effect contract clause\n- declares which effect layer/state transition the action or step drives\n- matching intent steps may reuse this from the action contract"; }
    if index == 5 { return "**class** - Passive nominal host type (value semantics)"; }
    if index == 6 { return "**effects** - Declaration-local effect contract tail\n- used in `with effects ...`\n- currently stays on the function/action declaration surface\n- not reused as part of the intent step contract pack"; }
    if index == 7 { return "**func** - Function declaration"; }
    if index == 8 { return "**import** - Import module"; }
    if index == 9 { return "**let** - Variable declaration"; }
    if index == 10 { return "**match** - Pattern matching expression"; }
    if index == 11 { return "**object** - Passive state-bearing object type; can react but does not initiate intent"; }
    if index == 12 { return "**parallel** - Parallel execution block"; }
    if index == 13 { return "**party** - Authority-bearing participant declaration"; }
    if index == 14 { return "**requires** - Ability contract clause\n- on `action`: part of the action contract pack\n- on `step`: explicit override or restatement of ability requirements"; }
    if index == 15 { return "**roster** - Party container with capacity constraints. Groups multiple parties (e.g., 4-party dungeon raid)"; }
    if index == 16 { return "**struct** - Value type (passed by value)"; }
    if index == 17 { return "**subject** - Identity-bearing host type\n- subject values are anchored handles, not plain copied values\n- subject actions can carry contract clauses like `requires`, `within`, `authorized by`, `causes`"; }
    if index == 18 { return "**tobject** - Transfer object. Boundary transfer data type"; }
    if index == 19 { return "**transfer** - Cross-zone handoff clause\n- `transfer: from -> to` can derive the step `where` and `using` contract from the target zone\n- diagnostics should explain both current contract and the derived target contract"; }
    if index == 20 { return "**using** - Intent step bound-zone alias clause\n- binds the step to a specific zone participant alias\n- may be derived from the `transfer` target contract"; }
    if index == 21 { return "**where** - Contract/location clause\n- in generics: type constraint surface\n- in intent steps: current zone contract surface"; }
    if index == 22 { return "**who** - Intent step participant clause\n- names which intent participant performs the step\n- may be inherited from a unique matching subject action"; }
    if index == 23 { return "**with** - Two different surfaces share this word\n- `with slot ... as ...` is a scoped binding/resource surface\n- `with effects ...` is a declaration-local effect contract\n- `with effects` is not part of the action -> step contract pack"; }
    if index == 24 { return "**within** - Action zone contract clause\n- fixes which zone type the action runs inside\n- matching intent steps may reuse this zone contract"; }
    if index == 25 { return "**Err(message)** - Create error Result"; }
    if index == 26 { return "**Log(value)** - Print value with newline\n- If argument is multiline string, it is normalized and emitted via banner-style output"; }
    if index == 27 { return "**LogBanner(text)** - Print banner text with indentation-normalized output"; }
    if index == 28 { return "**LogBlock(text)** - Print multiline block text with banner-style normalization"; }
    if index == 29 { return "**LogRaw(value)** - Print raw string value including embedded escapes/newlines"; }
    if index == 30 { return "**Ok(value)** - Create success Result"; }
    if index == 31 { return "**Unwrap(result)** - Extract value or panic"; }
    return "";
}

func LspHoverPresentationLanguageWordAt(index: Int) -> Bool {
    if index == 0 { return true; }
    if index == 1 { return true; }
    if index == 2 { return true; }
    if index == 3 { return true; }
    if index == 4 { return true; }
    if index == 5 { return true; }
    if index == 6 { return true; }
    if index == 7 { return true; }
    if index == 8 { return true; }
    if index == 9 { return true; }
    if index == 10 { return true; }
    if index == 11 { return true; }
    if index == 12 { return true; }
    if index == 13 { return true; }
    if index == 14 { return true; }
    if index == 15 { return true; }
    if index == 16 { return true; }
    if index == 17 { return true; }
    if index == 18 { return true; }
    if index == 19 { return true; }
    if index == 20 { return true; }
    if index == 21 { return true; }
    if index == 22 { return true; }
    if index == 23 { return true; }
    if index == 24 { return true; }
    if index == 25 { return false; }
    if index == 26 { return false; }
    if index == 27 { return false; }
    if index == 28 { return false; }
    if index == 29 { return false; }
    if index == 30 { return false; }
    if index == 31 { return false; }
    return false;
}

func LspHoverPresentationTextForWord(word: String) -> Option<String> {
    if word == "action" { return Some("**action** - Subject-host contract-bearing operation\n- carries an action contract pack: `within`, `requires`, `authorized by`, `causes`\n- matching intent steps may reuse this contract pack instead of repeating every clause"); }
    if word == "authority" { return Some("**authority** - Zone mutation authority declaration\n- `authority subjectSlot` names the participant; optional `requires Ability` adds capability evidence\n- this often overlaps with action/step `requires`, so diagnostics should explain when the requirement was inherited rather than written twice"); }
    if word == "authorized" { return Some("**authorized by** - Authority contract clause\n- required when a step/action mutates authority-bearing zone state or runs secure/effectful work inside an authority boundary\n- matching intent steps may reuse this from the action contract"); }
    if word == "by" { return Some("**by** - Authority clause tail\n- used in `authorized by` and domain lifecycle commands to name the approving participant"); }
    if word == "causes" { return Some("**causes** - Effect contract clause\n- declares which effect layer/state transition the action or step drives\n- matching intent steps may reuse this from the action contract"); }
    if word == "class" { return Some("**class** - Passive nominal host type (value semantics)"); }
    if word == "effects" { return Some("**effects** - Declaration-local effect contract tail\n- used in `with effects ...`\n- currently stays on the function/action declaration surface\n- not reused as part of the intent step contract pack"); }
    if word == "func" { return Some("**func** - Function declaration"); }
    if word == "import" { return Some("**import** - Import module"); }
    if word == "let" { return Some("**let** - Variable declaration"); }
    if word == "match" { return Some("**match** - Pattern matching expression"); }
    if word == "object" { return Some("**object** - Passive state-bearing object type; can react but does not initiate intent"); }
    if word == "parallel" { return Some("**parallel** - Parallel execution block"); }
    if word == "party" { return Some("**party** - Authority-bearing participant declaration"); }
    if word == "requires" { return Some("**requires** - Ability contract clause\n- on `action`: part of the action contract pack\n- on `step`: explicit override or restatement of ability requirements"); }
    if word == "roster" { return Some("**roster** - Party container with capacity constraints. Groups multiple parties (e.g., 4-party dungeon raid)"); }
    if word == "struct" { return Some("**struct** - Value type (passed by value)"); }
    if word == "subject" { return Some("**subject** - Identity-bearing host type\n- subject values are anchored handles, not plain copied values\n- subject actions can carry contract clauses like `requires`, `within`, `authorized by`, `causes`"); }
    if word == "tobject" { return Some("**tobject** - Transfer object. Boundary transfer data type"); }
    if word == "transfer" { return Some("**transfer** - Cross-zone handoff clause\n- `transfer: from -> to` can derive the step `where` and `using` contract from the target zone\n- diagnostics should explain both current contract and the derived target contract"); }
    if word == "using" { return Some("**using** - Intent step bound-zone alias clause\n- binds the step to a specific zone participant alias\n- may be derived from the `transfer` target contract"); }
    if word == "where" { return Some("**where** - Contract/location clause\n- in generics: type constraint surface\n- in intent steps: current zone contract surface"); }
    if word == "who" { return Some("**who** - Intent step participant clause\n- names which intent participant performs the step\n- may be inherited from a unique matching subject action"); }
    if word == "with" { return Some("**with** - Two different surfaces share this word\n- `with slot ... as ...` is a scoped binding/resource surface\n- `with effects ...` is a declaration-local effect contract\n- `with effects` is not part of the action -> step contract pack"); }
    if word == "within" { return Some("**within** - Action zone contract clause\n- fixes which zone type the action runs inside\n- matching intent steps may reuse this zone contract"); }
    if word == "Err" { return Some("**Err(message)** - Create error Result"); }
    if word == "Log" { return Some("**Log(value)** - Print value with newline\n- If argument is multiline string, it is normalized and emitted via banner-style output"); }
    if word == "LogBanner" { return Some("**LogBanner(text)** - Print banner text with indentation-normalized output"); }
    if word == "LogBlock" { return Some("**LogBlock(text)** - Print multiline block text with banner-style normalization"); }
    if word == "LogRaw" { return Some("**LogRaw(value)** - Print raw string value including embedded escapes/newlines"); }
    if word == "Ok" { return Some("**Ok(value)** - Create success Result"); }
    if word == "Unwrap" { return Some("**Unwrap(result)** - Extract value or panic"); }
    return None;
}

func LspHoverPresentationProjectionReady() -> Bool {
    if LspHoverPresentationLanguageWordCount() != 25 {
        return false;
    }
    if LspHoverPresentationBuiltinCount() != 7 {
        return false;
    }
    let index: Int = 0;
    while index < LspHoverPresentationCount() {
        let word: String = LspHoverPresentationWordAt(index);
        let text: String = LspHoverPresentationTextAt(index);
        if StringLength(word) == 0 || StringLength(text) == 0 {
            return false;
        }
        let lookup: Option<String> = LspHoverPresentationTextForWord(word);
        if !IsSome(lookup) || UnwrapOption(lookup) != text {
            return false;
        }
        index = index + 1;
    }
    return !IsSome(LspHoverPresentationTextForWord("not_a_hover_word"));
}
