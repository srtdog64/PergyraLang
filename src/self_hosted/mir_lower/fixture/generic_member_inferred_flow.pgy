class Box {
    let marker: Int;

    func Echo<T>(self, value: T) -> T {
        return value;
    }
}

func Main() -> Void {
    let box: Box = Box(0);
    let result: Int = box.Echo(box.Echo(41));
    Log(ToString(result));
}
