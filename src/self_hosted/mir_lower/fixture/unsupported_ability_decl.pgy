ability Arithmetic {
    func Add(self, rhs: Int) -> Int;
}

role IntMath for Int {
    impl Arithmetic {
        func Add(self, rhs: Int) -> Int {
            return 123;
        }
    }
}

func Main() -> Void {
    let a: Int = 1;
    let b: Int = 2;
    Log(a + b);
}
