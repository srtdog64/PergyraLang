Program:
  Vessel: HP
    Fields:
      current: Int
      max: Int
    Methods:
      Function: IsAlive
        Parameters:
          self
        Returns: Bool
        Body:
          Block:
            Return: (current > 0)
      Function: Hit
        Parameters:
          self
          dmg: Int
        Returns: Void
        Body:
          Block:
            Assign: current = (current - dmg)
  Function: Main
    Parameters:
    Body:
      Block:
        Let: a = HP(100, 100)
        a.Hit(30)
        Let: alive : Bool = a.IsAlive()
        Let: dead : Bool = (!alive)
        If: dead
          Then:
            Block:
              Log("dead")
          Else:
            Block:
              Log(("alive at " + ToString(a.current)))

