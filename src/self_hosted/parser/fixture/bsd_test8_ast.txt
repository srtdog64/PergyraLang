Program:
  Function: Main
    Parameters:
    Returns: Void
    Body:
      Block:
        Let: x = 1
        Let: running = true
        While: running
          Block:
            If: (x == 0)
              Then:
                Block:
                  Log("zero")
            If: (x == 1)
              Then:
                Block:
                  Log("one")
            Assign: running = false

