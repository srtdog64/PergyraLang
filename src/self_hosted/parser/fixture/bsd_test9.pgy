let running = true;
let turn = 1;
while running
{
    let event = Random(4);

    if event == 0
    {
        Log("zero");
    }

    if event == 1
    {
        Log("one");
    }

    turn = turn + 1;
    if turn >= 3
    {
        running = false;
    }
}
Log("done");
