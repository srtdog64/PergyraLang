Program:
  Function: Main
    Parameters:
    Returns: Void
    Body:
      Block:
        Let: ch : Channel<Int: Int> = Channel(4)
        Let: result : Slot<Int: Int> = ClaimSlot<Int>()
        Parallel:
          ChannelSend: ch <- 77
          Write(result, <-ch)
        Log(Read(result))
        Release(result)

