Program:
  Function: Main
    Parameters:
    Returns: Void
    Body:
      Block:
        Let: ch : Channel<Int: Int> = Channel(4)
        Let: result : Slot<Int: Int> = ClaimSlot<Int>()
        ChannelSend: ch <- 99
        Let: val : Int = <-ch
        Write(result, val)
        Log(Read(result))
        Release(result)

