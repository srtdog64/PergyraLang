Program:
  Function: Main
    Parameters:
    Returns: Void
    Body:
      Block:
        Let: parts : Array<String: String> = Split("apple,banana,cherry", ",")
        For: fruit in (null)..(null)
          Block:
            Log(fruit)
        Let: words : Array<String: String> = ["hello", "world", "pergyra"]
        Let: sentence : String = Join(words, " ")
        Log(sentence)
        Let: num : Int = ToInt("42")
        Log((num + 8))
        Let: pi : Float = ToFloat("3.14")
        Log(pi)
        Let: csv_line : String = "100,Alice,true"
        AST node type 6
        Let: id : Int = ToInt(id_str)
        Log(id)
        Log(name)

