# Semantic Substitution Track

Reserved for Pergyra-written semantic checks. Initial scope should stay tiny:
function signatures, local `let`, basic return typing, and explicit diagnostic
parity against the C semantic checker.
