// Generated by scripts/render_builtin_effect_registry.py; do not edit.
// Source: src/semantic/builtin_effect_registry.def.
import "effect_mask_owner.pgy";

func SemanticBuiltinFixedEffectMask(name: String) -> Int {
    if name == "Now" { return SemanticEffectMaskForWord("nondeterministic"); }
    if name == "Cancel" { return SemanticEffectMaskForWord("remote"); }
    if name == "Random" { return SemanticEffectMaskForWord("nondeterministic"); }
    if name == "SeedRandom" { return SemanticEffectMaskForWord("nondeterministic"); }
    if name == "ReadLine" { return SemanticEffectMaskForWord("nondeterministic"); }
    if name == "Sleep" { return SemanticEffectMaskForWord("nondeterministic"); }
    if name == "Print" { return SemanticEffectMaskForWord("io"); }
    if name == "StringLength" { return SemanticEffectMaskForWord("local"); }
    if name == "ToString" { return SemanticEffectMaskForWord("local"); }
    if name == "Log" { return SemanticEffectMaskForWord("local"); }
    if name == "Args" { return SemanticEffectMaskForWord("nondeterministic"); }
    if name == "Input" { return SemanticEffectMaskForWord("nondeterministic"); }
    if name == "CompilerArtifactAbort" { return SemanticEffectMaskForWord("io"); }
    if name == "CompilerArtifactBegin" { return SemanticEffectMaskForWord("io"); }
    if name == "CompilerArtifactCommit" { return SemanticEffectMaskForWord("io"); }
    if name == "CompilerArtifactWrite" { return SemanticEffectMaskForWord("io"); }
    if name == "DirWalk" { return SemanticEffectMaskJoin(SemanticEffectMaskForWord("io"), SemanticEffectMaskForWord("nondeterministic")); }
    if name == "FileExists" { return SemanticEffectMaskForWord("io"); }
    if name == "FileOpen" { return SemanticEffectMaskForWord("io"); }
    if name == "FileRead" { return SemanticEffectMaskForWord("io"); }
    if name == "FileWrite" { return SemanticEffectMaskForWord("io"); }
    if name == "FileClose" { return SemanticEffectMaskForWord("io"); }
    if name == "ReadFile" { return SemanticEffectMaskForWord("io"); }
    if name == "ReadStdin" { return SemanticEffectMaskJoin(SemanticEffectMaskForWord("io"), SemanticEffectMaskForWord("nondeterministic")); }
    if name == "WriteFile" { return SemanticEffectMaskForWord("io"); }
    if name == "Abs" { return SemanticEffectMaskForWord("local"); }
    if name == "Acos" { return SemanticEffectMaskForWord("local"); }
    if name == "Asin" { return SemanticEffectMaskForWord("local"); }
    if name == "Atan" { return SemanticEffectMaskForWord("local"); }
    if name == "Atan2" { return SemanticEffectMaskForWord("local"); }
    if name == "Ceil" { return SemanticEffectMaskForWord("local"); }
    if name == "CharAtN" { return SemanticEffectMaskForWord("local"); }
    if name == "CharCode" { return SemanticEffectMaskForWord("local"); }
    if name == "CheckedAdd" { return SemanticEffectMaskForWord("local"); }
    if name == "CheckedMul" { return SemanticEffectMaskForWord("local"); }
    if name == "Clamp" { return SemanticEffectMaskForWord("local"); }
    if name == "Concat" { return SemanticEffectMaskForWord("local"); }
    if name == "Contains" { return SemanticEffectMaskForWord("local"); }
    if name == "Cos" { return SemanticEffectMaskForWord("local"); }
    if name == "E" { return SemanticEffectMaskForWord("local"); }
    if name == "Exit" { return SemanticEffectMaskForWord("local"); }
    if name == "Exp" { return SemanticEffectMaskForWord("local"); }
    if name == "Floor" { return SemanticEffectMaskForWord("local"); }
    if name == "Join" { return SemanticEffectMaskForWord("local"); }
    if name == "Log10" { return SemanticEffectMaskForWord("local"); }
    if name == "Log2" { return SemanticEffectMaskForWord("local"); }
    if name == "Lower" { return SemanticEffectMaskForWord("local"); }
    if name == "MathLog" { return SemanticEffectMaskForWord("local"); }
    if name == "Max" { return SemanticEffectMaskForWord("local"); }
    if name == "Min" { return SemanticEffectMaskForWord("local"); }
    if name == "PI" { return SemanticEffectMaskForWord("local"); }
    if name == "Pow" { return SemanticEffectMaskForWord("local"); }
    if name == "Replace" { return SemanticEffectMaskForWord("local"); }
    if name == "Round" { return SemanticEffectMaskForWord("local"); }
    if name == "Sin" { return SemanticEffectMaskForWord("local"); }
    if name == "Split" { return SemanticEffectMaskForWord("local"); }
    if name == "Sqrt" { return SemanticEffectMaskForWord("local"); }
    if name == "StringConcat" { return SemanticEffectMaskForWord("local"); }
    if name == "StringContains" { return SemanticEffectMaskForWord("local"); }
    if name == "StringIndexOf" { return SemanticEffectMaskForWord("local"); }
    if name == "StringJoin" { return SemanticEffectMaskForWord("local"); }
    if name == "StringReplace" { return SemanticEffectMaskForWord("local"); }
    if name == "StringSplit" { return SemanticEffectMaskForWord("local"); }
    if name == "StringTrim" { return SemanticEffectMaskForWord("local"); }
    if name == "SubContains" { return SemanticEffectMaskForWord("local"); }
    if name == "SubContainsWithLen" { return SemanticEffectMaskForWord("local"); }
    if name == "SubEquals" { return SemanticEffectMaskForWord("local"); }
    if name == "SubEqualsWithLen" { return SemanticEffectMaskForWord("local"); }
    if name == "SubIndexOf" { return SemanticEffectMaskForWord("local"); }
    if name == "SubIndexOfWithLen" { return SemanticEffectMaskForWord("local"); }
    if name == "SubStartsWith" { return SemanticEffectMaskForWord("local"); }
    if name == "SubStartsWithLen" { return SemanticEffectMaskForWord("local"); }
    if name == "Substring" { return SemanticEffectMaskForWord("local"); }
    if name == "SubstringWithLen" { return SemanticEffectMaskForWord("local"); }
    if name == "Tan" { return SemanticEffectMaskForWord("local"); }
    if name == "ToFloat" { return SemanticEffectMaskForWord("local"); }
    if name == "ToInt" { return SemanticEffectMaskForWord("local"); }
    if name == "ToLower" { return SemanticEffectMaskForWord("local"); }
    if name == "ToUpper" { return SemanticEffectMaskForWord("local"); }
    if name == "Trim" { return SemanticEffectMaskForWord("local"); }
    if name == "Upper" { return SemanticEffectMaskForWord("local"); }
    if name == "ArrayDropOwnedStrings" { return SemanticEffectMaskForWord("local"); }
    if name == "ArrayLength" { return SemanticEffectMaskForWord("local"); }
    if name == "ArrayPop" { return SemanticEffectMaskForWord("local"); }
    if name == "ArrayPush" { return SemanticEffectMaskForWord("local"); }
    if name == "ArrayPushOwnedString" { return SemanticEffectMaskForWord("local"); }
    if name == "ArrayReverse" { return SemanticEffectMaskForWord("local"); }
    if name == "ArraySet" { return SemanticEffectMaskForWord("local"); }
    if name == "ArraySort" { return SemanticEffectMaskForWord("local"); }
    if name == "CompilerRetireArrayStorage" { return SemanticEffectMaskForWord("local"); }
    if name == "ListGet" { return SemanticEffectMaskForWord("local"); }
    if name == "ListNew" { return SemanticEffectMaskForWord("local"); }
    if name == "ListPush" { return SemanticEffectMaskForWord("local"); }
    if name == "ListRemove" { return SemanticEffectMaskForWord("local"); }
    if name == "ListSet" { return SemanticEffectMaskForWord("local"); }
    if name == "ListSize" { return SemanticEffectMaskForWord("local"); }
    if name == "QueueEmpty" { return SemanticEffectMaskForWord("local"); }
    if name == "QueueNew" { return SemanticEffectMaskForWord("local"); }
    if name == "QueuePop" { return SemanticEffectMaskForWord("local"); }
    if name == "QueuePush" { return SemanticEffectMaskForWord("local"); }
    if name == "QueueSize" { return SemanticEffectMaskForWord("local"); }
    if name == "SetAdd" { return SemanticEffectMaskForWord("local"); }
    if name == "SetHas" { return SemanticEffectMaskForWord("local"); }
    if name == "SetNew" { return SemanticEffectMaskForWord("local"); }
    if name == "SetRemove" { return SemanticEffectMaskForWord("local"); }
    if name == "SetSize" { return SemanticEffectMaskForWord("local"); }
    if name == "SetValues" { return SemanticEffectMaskForWord("local"); }
    if name == "SliceCopy" { return SemanticEffectMaskForWord("local"); }
    if name == "MapGet" { return SemanticEffectMaskForWord("local"); }
    if name == "MapHas" { return SemanticEffectMaskForWord("local"); }
    if name == "MapKeys" { return SemanticEffectMaskForWord("local"); }
    if name == "MapNew" { return SemanticEffectMaskForWord("local"); }
    if name == "MapRemove" { return SemanticEffectMaskForWord("local"); }
    if name == "MapSet" { return SemanticEffectMaskForWord("local"); }
    if name == "MapSize" { return SemanticEffectMaskForWord("local"); }
    return -1;
}
