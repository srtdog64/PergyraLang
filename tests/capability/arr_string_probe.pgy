// Diagnostic for bug #44: does Array<String> itself crash on LLVM, or only
// Args()? A plain Array<String> literal with no process-args involvement.
let a: Array<String> = ["x", "y", "z"];
let n: Int = 0;
for s in a {
    n = n + 1;
}
Log("arr-string ok");
