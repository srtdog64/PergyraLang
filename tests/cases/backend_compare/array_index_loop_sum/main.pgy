func Main() -> Void {
    let arr: Array<Int> = [10, 20, 30, 40];
    let total: Int = 0;
    let i: Int = 0;
    while i < 4 {
        total = total + arr[i];
        i = i + 1;
    }
    Log(total);
}
