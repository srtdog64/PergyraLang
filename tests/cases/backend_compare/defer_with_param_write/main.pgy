func Compute(seed: Int) -> Int {
    let s: Slot<Int> = ClaimSlot();
    Write(s, seed);
    defer { Log(Read(s)); };
    Write(s, Read(s) * 2);
    Write(s, Read(s) + 5);
    return Read(s);
}

func Main() -> Void {
    let r: Int = Compute(7);
    Log(r);
}
