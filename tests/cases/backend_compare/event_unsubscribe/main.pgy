event OnScore(points: Int);

func RecordScore(points: Int) -> Void {
    Log("score=" + ToString(points));
}

func Main() -> Void {
    OnScore += RecordScore;
    OnScore(7);
    OnScore -= RecordScore;
    OnScore(9);
    Log("done");
}
