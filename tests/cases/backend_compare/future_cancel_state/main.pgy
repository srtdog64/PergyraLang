func Worker() -> Int {
    return 1;
}

async func Main() -> Void {
    Log(IsCancelled());
    let pending: Future<Int> = spawn Worker();
    Log(Cancel(pending));
    let joined: Int = await pending;
}
