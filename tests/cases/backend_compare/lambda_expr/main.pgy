func Apply(f: func(Int) -> Int, x: Int) -> Int {
    return f(x);
}

func Main() -> Void {
    let double: func(Int) -> Int = (x: Int) => x * 2;
    Log(Apply(double, 5));
    Log(Apply((x: Int) => x + 10, 3));
}
