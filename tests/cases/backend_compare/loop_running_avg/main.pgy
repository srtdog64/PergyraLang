func Main() -> Void {
    let xs: Array<Int> = [10, 20, 30, 40, 50, 60, 70];
    let sum: Int = 0;
    let i: Int = 0;
    while i < ArrayLength(xs) {
        sum = sum + xs[i];
        let count: Int = i + 1;
        let avg: Int = sum / count;
        Log(avg);
        i = i + 1;
    }
}
