func Larger(a: Int, b: Int) -> Int {
    if a > b {
        return a;
    }
    return b;
}

func Main() -> Void {
    let acc: Slot<Int> = ClaimSlot();
    Write(acc, 0);
    Write(acc, Larger(Read(acc), 5));
    Write(acc, Larger(Read(acc), 3));
    Write(acc, Larger(Read(acc), 8));
    Write(acc, Larger(Read(acc), 1));
    Log(Read(acc));
}
