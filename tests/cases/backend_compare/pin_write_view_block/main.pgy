func Main() -> Void {
    let scores: Slot<Int> = ClaimSlot();
    Write(scores, 1);

    pin scores as view: WriteView<Int> {
        Write(view, 7);
    }

    let value: Int = Read(scores);
    Log(value);
}
