func Main() -> Void {
    let seen: Set<Int> = SetNew();
    SetAdd(seen, 7);
    SetAdd(seen, 9);
    Log(SetHas(seen, 7));
    SetRemove(seen, 7);
    Log(SetHas(seen, 7));
    Log(SetSize(seen));
}
