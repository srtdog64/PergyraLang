subject Vec2 {
    let x: Int;
    let y: Int;
}

func Recur(ref slot: Slot<Vec2>) -> Void {
    let unrelated: Int = 7;
    Recur(slot);
    Write(slot, Vec2(1, 2));
}

func Main() -> Void {
    let slot: Slot<Vec2> = Vec2(0, 0);
    Recur(slot);
    Release(slot);
}
