// None carries no payload type, so no argument fixes T. The checker refuses
// the call; it used to pass semantic and fail in MIR lowering.
func TakeOpt<T>(o: Option<T>) -> Int {
    if IsSome(o) {
        return 1;
    }
    return 0;
}

func Main() -> Void {
    Log(ToString(TakeOpt(None)));
}
