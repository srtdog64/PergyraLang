func Main() -> Void {
    let result: Allocator = AllocatorResult();
    let text: TextBuilder = TextBuilderNew(8);
    if true {
        TextBuilderAppend(text, "nested");
    }
    let finished: String = TextBuilderFinish(text, result);
    Print(finished);
    AllocatorDestroy(result);
}
