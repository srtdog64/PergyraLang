import "callable_capability_fixture.pgy";

func ClockBump(value: Int) -> Int with caps clock {
    let observed: Int = Now();
    return value + 1;
}
func Keep(operation: func(Int) -> Int, value: Int) -> Int { return value; }
func Main() -> Void with caps random { Log(Keep(ClockBump, 42)); }
