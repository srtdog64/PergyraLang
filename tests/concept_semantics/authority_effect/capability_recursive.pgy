func First(n: Int) -> Int with caps clock {
    if n > 0 { return Second(n - 1); }
    return Now();
}
func Second(n: Int) -> Int with caps clock { return First(n); }
func Main() -> Void { let stamp: Int = First(2); Log("ok"); }
