func MapKeys(values: HashMap<String, Int>) -> Array<String> {
    return [];
}

func Main() -> Void {
    let values: HashMap<String, Int> = MapNew();
    let keys: Array<String> = MapKeys(values);
    ArrayDropOwnedStrings(keys);
}
