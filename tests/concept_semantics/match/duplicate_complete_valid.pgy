enum Shape { Dot, Line(Int), Rect(Int, Int) }
func Area(s: Shape) -> Int {
    match s {
        case Dot: return 0;
        case Dot: return 1;
        case Line(n): return n;
        case Rect(w, h): return w * h;
    }
}
func Main() -> Void { Log(Area(Rect(2, 3))); }
