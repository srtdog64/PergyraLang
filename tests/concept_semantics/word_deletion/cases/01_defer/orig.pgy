func Work(flag: Bool) -> Int {
    defer { Log("cleanup"); };
    Log("open");
    if flag {
        return 1;
    }
    Log("body");
    return 0;
}
func Main() -> Void {
    Log(ToString(Work(false)));
    Log(ToString(Work(true)));
}
