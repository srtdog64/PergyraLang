subject Counter {
    let mut value: Int;
    func Bump(self) -> Void {
        self.value = self.value + 1;
    }
}
func Main() -> Void {
    let c: Counter = Counter(0);
    c.Bump();
    c.Bump();
    Log(ToString(c.value));
}
