subject Worker {
    let mut jobs: Int;
    let ready: Bool;
    func Work(self) -> Void { if ready { jobs = jobs + 1; } }
}
func Main() -> Void {
    let w: Worker = Worker(0, true);
    w.Work();
    Log(ToString(w.jobs));
}
