private class Inner { let value: Int; func Next(self) -> Int { return value + 1; } }
public subject Runner { let inner: Inner; func Run(self) -> Int { return inner.Next(); } }
func Main() -> Void {
    let r = Runner(Inner(4));
    Log(ToString(r.Run()));
}
