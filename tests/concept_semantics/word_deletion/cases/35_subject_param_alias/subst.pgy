subject Acct {
    let mut bal: Int;
    func Debit(self) -> Void { self.bal = self.bal - 10; }
}
func Pay(inout acct: Acct) -> Void { acct.Debit(); }
func Main() -> Void {
    let mut a = Acct(100);
    Pay(a);
    Log(ToString(a.bal));
}
