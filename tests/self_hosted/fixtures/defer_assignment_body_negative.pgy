// The default route rebuilds Log and direct-call defer statements only; an
// assignment in a defer body is refused there (docs/205 F5).
func Main() -> Void {
    let count: Int = 1;
    defer {
        Log("first");
        count = 3;
    }
    Log(ToString(count));
}
