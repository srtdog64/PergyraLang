subject Player {
    let hp: Int;
}

zone CounterZone {
    subject slot player: Player
}

func Main() -> Void {
    let counter: CounterZone = CounterZone(Player(7));
    counter = CounterZone(Player(9));
    Log(counter.player.hp);
}
