// A field passed inout was a lost update on native C: this printed 11.
struct Pair {
    let a: Int;
    let b: Int;
}

func Both(inout x: Int, inout y: Int) -> Void {
    x = x + 1;
    y = y + 10;
}

func Main() -> Void {
    let s: Pair = Pair(1, 2);
    Both(s.a, s.a);
    Log(s.a);
}
