subject Player {
    let hp: Int;
}

effect Burn for bearer: Player { }

zone Arena {
    subject slot player: Player
    effect slot burn: Burn
    apply burn to 42
}

func Main() -> Void {
    Log("unreached");
}
