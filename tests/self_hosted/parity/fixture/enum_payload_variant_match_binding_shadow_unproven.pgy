enum InnerTag {
    Aster(Int),
    Brume(Int)
}

enum OuterTag {
    Wrap(InnerTag)
}

func Main() -> Void {
    let inner: InnerTag = Aster(74);
    let outer: OuterTag = Wrap(Brume(75));
    match outer {
        case Wrap(inner):
            Log(inner.Aster._0);
    }
}
